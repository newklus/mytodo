# MyTodo 요구사항 명세서

> 마지막 갱신: 2026-09-08 (성능 최적화 — 10절 "성능 · 실행 환경" 신설. 기능 변경 없음)
> 이 문서는 프로젝트 요구사항의 **단일 기준(SSOT)** 입니다. 8절의 변경 관리 원칙을 반드시 지켜주세요.

## 1. 프로젝트 배경

- 클로드(Claude Code) 사용법 학습 + 효율적인 프롬프트 작성법을 익히기 위한 실습 프로젝트 (`BG.md` 참고)
- 실습 소재로 **회사 업무에 실제로 쓸 수 있는 개인용 TODO 앱**을 만든다.

## 2. 목적 및 범위

- **용도**: 개인용 (본인 업무 관리). 팀 협업 기능(공유, 권한, 배정 등)은 범위 밖.
- **목표 수준**: 학습용 장난감이 아니라 실제 회사 업무에 쓸 수 있는 수준의 완성도.
- **벤치마크**: Todoist, Things3, TickTick, Any.do 등 개인 생산성 툴의 핵심 기능(우선순위, 오늘/예정 뷰, 반복 일정)을 차용하고, 여기에 회사 업무 특화 기능인 **주간보고 자동화**를 추가한다.

## 3. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js 16 (App Router, TypeScript, `src/` 디렉토리) |
| 스타일 | Tailwind CSS 4 |
| DB | SQLite (`dev.db`, 프로젝트 루트) |
| ORM | Prisma **7.10.0 고정** (8.x는 아직 RC라 사용하지 않음) + `@prisma/adapter-better-sqlite3` 드라이버 어댑터 필수 |
| 데이터 변경 | Next.js Server Actions만 사용 (별도 REST API 없음) |
| 배포(예정) | Vercel |

## 4. 데이터 모델 (`prisma/schema.prisma`)

- **Project**: `id, name, color?, createdAt` — `tasks[]`
- **Tag**: `id, name(unique), color?` — `tasks: TaskTag[]`
- **Task**: `id, title, description?, dueDate?, priority(1~4, 기본 4), completed, completedAt?, recurrence?("DAILY"|"WEEKLY"|"MONTHLY"), order, projectId?, parentId?(자기참조)` — `subtasks[], tags: TaskTag[]`
- **TaskTag**: Task-Tag 조인 테이블 (`taskId + tagId` 복합키)
- **PriorityColor**: `priority(1~4, PK), color` — 우선순위별 커스텀 색상 (레코드 없는 우선순위는 `DEFAULT_PRIORITY_COLORS` 기본값 사용)

## 5. 구현 완료 기능 (Phase 1 · MVP) ✅

### 5.1 태스크 CRUD
- 생성: 제목(필수) / 마감일 / 우선순위(P1~P4) / 프로젝트 / 반복 규칙 / 태그(쉼표 구분)
- **퀵애드(inline 폼 + `n` 팝업)의 제목 입력창은 `@프로젝트`, `#태그` 인라인 멘션을 지원** (`SmartTitleInput` 컴포넌트, 태스크 편집 화면은 기존 드롭다운 방식 그대로 유지 — 여기는 적용 안 함)
  - `@`나 `#`을 입력하면 그 순간부터 뒤에 붙는 글자로 실시간 필터링된 드롭다운이 뜸 (기존 프로젝트/태그 중 이름에 포함되는 것 최대 6개)
  - 정확히 일치하는 기존 항목이 없으면 목록 맨 아래 "+ 새로 만들기: {입력값}" 옵션이 추가됨. 태그는 실제 태스크 생성 시 기존 `connectOrCreate` 흐름으로 자동 등록되고, 프로젝트는 선택 즉시 `findOrCreateProject` 서버 액션으로 미리 생성(또는 기존 항목 매칭)해서 id를 받아옴 — 색은 항상 랜덤 배정
  - 방향키(↑↓)로 후보 이동(강조는 다크/라이트 모두 대비가 뚜렷한 반전 배경 스타일). **Tab**은 드롭다운에서 강조된 항목 하나만 콕 집어 선택. Esc는 드롭다운만 닫기(팝업 자체는 안 닫힘)
  - **Enter는 커서 위치와 상관없이 텍스트 전체를 훑어서 남아있는 `@단어`/`#단어`를 한 번에 전부 칩으로 확정**한다 (`resolveAllMentions`) — 예를 들어 `제목 @프로젝트 #태그`까지 한 번에 타이핑하고 Enter 한 번만 누르면 프로젝트·태그가 동시에 확정되고 제목만 깨끗하게 남으며, 이 Enter는 제출로 이어지지 않는다. 확정할 게 더는 없을 때(=제목이 이미 깨끗할 때) 눌러야 그다음 Enter가 실제로 폼을 제출함
  - **주의(세부 동작 차이) — Tab과 Enter는 매칭 방식이 다름**: 드롭다운에서 **Tab**은 화면에 강조된 항목을 그대로 선택하므로 "프로젝" 같은 부분 입력으로도 강조된 기존 "프로젝트A"를 골라 연결할 수 있다. 반면 **Enter**(=`resolveAllMentions`)는 드롭다운을 보지 않고 텍스트에서 뽑아낸 단어와 기존 이름을 **대소문자만 무시한 완전일치**로 재검색한다 — 그래서 "프로젝"까지만 입력한 채 Enter를 누르면 기존 "프로젝트A"에 연결되지 않고 "프로젝"이라는 **새 프로젝트가 생성**된다. 기존 항목에 연결하려면 이름을 끝까지 입력하거나 Tab으로 선택해야 함
  - `@`/`#`는 **앞이 공백이거나 문장 시작일 때만** 멘션으로 인식됨(정규식 `(^|\s)[@#]\S+`) — 예: `hello@world` 처럼 앞에 공백이 없으면 멘션으로 파싱되지 않음(이메일 등 오인식 방지). 클라이언트(`parseAllMentions`)와 서버 안전망(`extractMentions`)이 동일한 규칙을 씀
  - 선택하면 `@검색어`/`#검색어` 텍스트는 입력창에서 바로 사라지고, 그 옆에 칩(프로젝트 칩 1개 + 태그 칩 여러 개, × 버튼으로 제거 가능)으로 표시됨 — **입력값 결과(제목)에는 @/# 텍스트가 절대 남지 않음**
  - 새로 만드는 프로젝트·태그 이름은 **입력한 대소문자 그대로** 등록됨 (검색/일치 비교만 대소문자 무시, 실제 생성 라벨은 원문 그대로 사용)
  - 새로 생성되는 프로젝트·태그의 색상은 완전 임의 색이 아니라 **정해진 7색 팔레트 중 무작위 하나** (`PALETTE`, `src/lib/colors.ts`: 빨강 `#ef4444`/주황 `#f97316`/노랑 `#eab308`/초록 `#22c55e`/파랑 `#3b82f6`/보라 `#8b5cf6`/분홍 `#ec4899`)
  - 드롭다운 항목·칩에 표시되는 이름이 24자보다 길면 중간을 `…`으로 생략 (`truncateMiddle`, `src/lib/truncate.ts`)
  - `@`로 새 프로젝트를 만드는 동안은 서버 왕복(생성/조회) 때문에 아주 짧게 대기 시간이 생기는데, 이 사이에 바로 제출하면 프로젝트 연결 없이 태스크가 생성될 수 있어서 **생성 중에는 "추가" 버튼이 비활성화**됨("프로젝트 생성 중…" 표시). `onPendingChange` 콜백으로 부모 폼에 진행 상태를 알려줌
  - **서버 쪽 안전망**: 드롭다운으로 확정하지 않고 `@단어`/`#단어`가 제목에 그대로 남은 채 제출돼도, `createTask` 서버 액션의 `extractMentions()`가 제목에서 그 토큰들을 다시 뽑아내 프로젝트/태그로 연결하고 제목에서는 지움 (`src/lib/actions/tasks.ts`). 클라이언트 드롭다운을 전혀 안 써도 `@`/`#` 문법 자체는 항상 동작함
  - **`+N` 마감일 단축입력**: 제목에 `+숫자`를 쓰면(앞뒤가 공백/문장 끝) N일 뒤 날짜가 마감일로 자동 설정되고 `+N` 텍스트는 제목에서 지워짐 (`+1`=내일, `+3`=3일 뒤). `src/lib/plusDate.ts`의 `parsePlusDate()` 순수함수를 클라이언트(`SmartTitleInput`, 실시간으로 마감일 입력창에 미리 채워줌)와 서버(`createTask`의 안전망, 명시적으로 마감일을 고른 게 없을 때만 폴백) 양쪽에서 동일하게 사용
    - `+N`도 `@`/`#`와 동일하게 **앞뒤가 공백/문장 경계일 때만** 인식되고(정규식에 `/g` 없이 **첫 번째 매치 하나만** 치환), 날짜 선택기(`<input type="date">`)에 사용자가 이미 값을 직접 골랐으면 `+N`이 있어도 **절대 덮어쓰지 않음** (명시적 선택이 항상 우선)
- 수정: 태스크 클릭 → 인라인 편집 폼 (제목/설명(메모)/마감일/우선순위/프로젝트/반복/태그)
- **저장 버튼 없음 — 포커스가 폼 밖으로 나가면 자동 저장됨** (`onBlur`, `relatedTarget`이 폼 안이면 무시). 값이 원래 태스크와 동일하면 저장 호출 없이 그냥 보기 화면으로 돌아가고, 하나라도 바뀌었으면 자동으로 `updateTask` 호출 후 보기 화면으로 전환. "취소" 버튼은 남겨둬서 명시적으로 변경을 버리고 닫을 수 있음
- **한 번에 하나의 태스크만 편집 모드일 수 있음**: 편집 중인 태스크 A의 제목을 클릭해 편집 폼을 연 채로 다른 태스크 B의 제목을 클릭하면, A가 먼저 위 자동저장 로직을 거쳐 보기 화면으로 닫힌 뒤 B가 편집 모드로 열림. 태스크 제목/서브태스크 제목 모두 클릭 시 `document.activeElement.blur()`를 먼저 호출해서 이전 폼의 `onBlur`가 확실히 트리거되도록 하고, 편집 폼 제목 입력창에는 `autoFocus`를 둬서 다음 클릭 때 blur 대상이 정확히 잡히게 함 (`TaskItem.tsx`)
- 메모(설명) 필드는 여러 줄 입력 가능한 textarea (`rows=3`). 메모가 있으면 목록에서 📝 아이콘으로 표시 (호버 시 내용 미리보기)
- **서브태스크에는 메모 필드를 두지 않음** (의도적 결정): 서브태스크는 빠른 체크리스트 용도로 가볍게 유지하고, 자세한 내용이 필요하면 메인 태스크의 메모를 쓰거나 별도 태스크로 승격시키는 쪽으로 유도

### 5.1.2 다중 선택 & 완료/삭제 (최상위 태스크만, `src/components/TaskList.tsx`)
- 행 맨 앞의 원 아이콘은 더 이상 완료 토글이 아니라 **다중 선택 체크박스**. 테두리는 항상 우선순위 색, 선택되면 그 **우선순위 색으로 배경까지 채워짐** (검정 아님)
- 완료/취소·삭제는 행 오른쪽의 **"완료"/"삭제" 버튼**으로 실행 (기존에 삭제 버튼이 있던 자리에 나란히 배치). 완료 버튼은 이미 완료된 태스크면 "완료 취소"로 라벨이 바뀌어 다시 미완료로 되돌릴 수 있음. 둘 다 클릭 시 확인창 1회
  - 미완료 태스크에서는 마우스를 올려야 나타남(호버 리빌), **완료된 태스크에서는 항상 보임** — 완료 뷰에서 "완료 취소"를 찾기 어렵다는 피드백을 받아 완료된 항목은 상시 노출로 바꿈
- 하나 이상 선택되면 목록 위에 "N개 선택됨 · d 완료 · r 삭제" 안내 바가 뜸
- **`d`/`r` 단축키**: 선택된 태스크가 하나 이상 있을 때만 동작 (입력 필드에 포커스 없을 때, 수정자 키 없이). `d`=선택된 항목 전체 완료 처리, `r`=선택된 항목 전체 삭제. 실행 전 확인창 1회 ("선택한 N개 작업을 완료 처리/삭제할까요?"), 확인 즉시 선택 해제
  - N개를 골라도 서버 호출은 **한 번**이다 (`completeTasks`/`deleteTasks` 일괄 액션, 10절 참고). 선택 개수와 무관하게 DB 왕복 1회 · 화면 갱신 1회로 끝난다
- **완료/완료취소/삭제한 행은 서버 응답을 기다리지 않고 즉시 목록에서 사라진다** (`useOptimistic`). 완료 뷰에서는 완료 취소한 것이, 나머지 뷰에서는 완료 처리한 것이 그 뷰의 조건을 벗어나므로 어느 쪽이든 결과는 같다. 서버 처리가 끝나면 실제 목록으로 자연히 대체되고, 실패하면 원래대로 되돌아온다. 서브태스크의 완료 토글·삭제도 동일하게 즉시 반영됨
- 서브태스크의 원 아이콘은 그대로 완료 토글 (다중 선택 대상 아님)

### 5.1.1 키보드 단축키
- **`n`**: 어디서든(입력 필드에 포커스가 없을 때) 누르면 새 할일 팝업이 뜸 (`QuickAddModal` 컴포넌트, 모든 페이지에 마운트됨). 제목 입력창에 자동 포커스
- **`Enter`**: 새 할일 팝업과 태스크 인라인 편집 폼의 제목/태그 등 한 줄 입력 필드에서 저장/추가 실행 — 폼에 submit 버튼이 있어서 브라우저 기본 동작으로 되는 것이라 별도 JS 처리 없음
- **`Ctrl+Enter`** (Mac은 `Cmd+Enter`): 태스크 인라인 편집 폼에서 어느 필드에 있든 저장 — 특히 메모(textarea)에서는 일반 Enter가 줄바꿈으로 쓰이기 때문에 이 단축키가 필요함. (새 할일 팝업에는 메모 필드가 없어서 이 단축키를 안 둠, 그냥 Enter로 충분)
- **`Esc`**: 새 할일 팝업 닫기 (편집 폼은 기존처럼 취소 버튼으로 닫음). 팝업 바깥의 반투명 배경을 클릭해도 동일하게 닫힘(`QuickAddModal`의 오버레이 `onMouseDown`, 이벤트 타깃이 오버레이 자신일 때만 — 팝업 내부 클릭은 안 닫힘)
- **인라인 폼(`QuickAddForm`)과 팝업(`QuickAddModal`)의 입력 상태 초기화 방식이 다름**: `QuickAddForm`은 태스크 추가 후에도 화면에 계속 남아있는 폼이라 `SmartTitleInput`에 `key={resetKey}`를 주고 제출할 때마다 `resetKey`를 증가시켜 **컴포넌트를 통째로 리마운트**해서 칩/텍스트 상태를 지운다. `QuickAddModal`은 닫힐 때 `if (!open) return null`로 아예 언마운트되므로 다음에 열 때 자연히 빈 상태로 새로 마운트됨(별도 리셋 로직 불필요)
- **`d`/`r`**: 다중 선택된 태스크 일괄 완료/삭제 (5.1.2 참고, 확인창 1회)
- 삭제: 브라우저 confirm 확인 후 삭제

### 5.2 뷰 / 필터
- 사이드바 표시 순서: **전체 → 오늘 → 예정 → 완료**
- **전체**: `completed=false` (마감일 무관). **일정순(마감일 오름차순, 마감일 없는 태스크는 맨 뒤)으로 정렬** — 마감일 없는 태스크도 여기서는 항상 보이도록 해서 눈에 안 띄어 잊히는 것을 방지
- **오늘**: `completed=false AND dueDate <= 오늘 23:59:59` (마감일 없는 태스크는 표시 안 됨). 우선순위 → 마감일 순 정렬
- **예정**: `completed=false AND dueDate > 오늘 23:59:59`. 우선순위 → 마감일 순 정렬
- **완료**: `completed=true`. 우선순위 → 마감일 순 정렬
- 위 4개 뷰 모두 서브태스크(`parentId != null`)는 목록에서 제외하고, 프로젝트 사이드바에서 프로젝트별 추가 필터 가능
- 페이지 진입 시 기본 뷰는 여전히 **오늘** (`?view` 파라미터 없을 때)

### 5.2.1 레이아웃: 목록 / 칸반 3종 (`?layout=list|priority|project|tag`, 기본 `list`)
- 상단에 "목록 / 우선순위별 / 프로젝트별 / 태그별" 토글 (헤딩 오른쪽)
- **우선순위별**: P1~P4 4개 컬럼
- **프로젝트별**: 프로젝트마다 컬럼 + "프로젝트 없음" 컬럼
- **태그별**: 태그마다 컬럼 + "태그 없음" 컬럼 — 태스크에 태그가 여러 개면 해당하는 모든 컬럼에 카드가 중복으로 나타남 (Notion 스타일 다중 그룹 표시)
- 위 3개 칸반 모두 현재 뷰·프로젝트 필터가 그대로 적용된 상태에서 재배열만 하는 것 (`src/components/TaskList.tsx`). 각 컬럼 안의 카드는 목록 뷰와 동일한 `TaskItem` 컴포넌트를 그대로 재사용 — 다중 선택/완료·삭제/서브태스크 드래그 앤 드롭 전부 컬럼을 넘나들며 동일하게 동작

### 5.3 프로젝트
- 사이드바에서 생성, 색상 랜덤 자동 배정
- 태스크에 프로젝트 배정 가능 (미배정 허용)
- **수동 삭제 버튼 없음 — 참조하는 태스크가 하나도 없어지면 자동 삭제됨**: 태스크 수정/삭제, 서브태스크를 다른 부모로 이동시킬 때 이전 `projectId`를 기준으로 `prisma.task.count({ where: { projectId } })`가 0이면 그 프로젝트를 자동 삭제 (`cleanupUnusedProjectAndTags`, `src/lib/cleanupUnused.ts`). 완료/미완료 상관없이 태스크가 하나라도 참조 중이면 삭제되지 않음

### 5.4 태그
- 쉼표로 여러 개 입력 → 없으면 자동 생성 후 연결, 새로 생성될 때 프로젝트처럼 랜덤 색상 자동 배정
- 태스크 카드에 색상 점 + `#태그` pill로 표시
- 사이드바 "⚙ 설정"(접이식, 기본 닫힘) 안 "태그 색상" 섹션에서 전체 태그 목록을 색상 점과 함께 보여주고, 점을 클릭하면 네이티브 색상 피커로 색을 바로 바꿀 수 있음 (`TagColorPicker` 컴포넌트, `updateTagColor` 서버 액션). **저장 버튼·확인창 없이 색을 고르는 즉시(`onChange`) 바로 반영됨** (네이티브 `<input type="color">`는 팔레트를 최종 확정할 때만 `onChange`가 발생하므로 드래그 중 매번 서버 호출이 일어나진 않음)
- 태그별로 묶어서 보는 건 5.2.1의 "태그별" 칸반으로 가능 (전통적인 필터 드롭다운은 아직 없음)
- **수동 삭제 버튼 없음 — 참조하는 태스크가 하나도 없어지면 자동 삭제됨**: 프로젝트와 동일한 방식으로, 태스크 수정/삭제 시 이전에 달려 있던 태그들 각각에 대해 `prisma.taskTag.count({ where: { tagId } })`가 0이면 그 태그를 자동 삭제 (완료/미완료 태스크 어느 쪽에서도 더 이상 안 쓰일 때만)

### 5.5 서브태스크
- 태스크당 **1단계** 체크리스트 (서브태스크의 서브태스크는 UI 미지원 — 스키마는 무제한 자기참조 지원)
- 개별 완료 토글 / 삭제, 자체 마감일·프로젝트·태그 없음(부모의 `projectId`만 상속)
- 부모 완료 시 서브태스크는 자동完료 처리되지 않음 (독립적으로 남음)
- 제목 클릭 시 인라인 텍스트 수정 가능 (`renameTask` 서버 액션, Enter로 저장·Escape로 취소)
- **드래그 앤 드롭으로 다른 상위 태스크로 이동 가능**: 행 맨 앞의 `⠿` 드래그 핸들 아이콘만 드래그 소스로 동작 (행 전체를 드래그 대상으로 하면 텍스트 클릭/수정과 충돌해서 핸들로 분리함). 다른 (최상위) 태스크에 드롭하면 그 태스크의 하위로 옮겨짐 (`moveSubtask` 서버 액션). 이동 시 `projectId`는 새 부모 태스크 것으로 다시 상속됨. 네이티브 HTML5 Drag and Drop API 사용, 별도 라이브러리 없음

### 5.6 반복 일정
- 옵션: 없음 / 매일 / 매주 / 매월
- 마감일이 있는 반복 태스크를 완료 처리하면: **원본은 완료 상태로 보존**(완료 이력 유지) + **다음 회차를 새 태스크로 자동 생성** (제목/설명/우선순위/프로젝트/반복 규칙만 복사, 태그·서브태스크는 복사 안 함)
- 마감일 없는 반복 태스크는 완료해도 다음 회차를 만들지 않음

### 5.7 우선순위 색상
- P1~P4 기본 색상: 빨강/주황/파랑/회색 (`DEFAULT_PRIORITY_COLORS`, `src/lib/priorityColors.ts`)
- 사이드바 "⚙ 설정"(접이식, 기본 닫힘) 안 "우선순위 색상" 섹션에서 P1~P4 각각 네이티브 색상 피커로 커스텀 가능 (`PriorityColorPicker` 컴포넌트, `updatePriorityColor` 서버 액션 → `PriorityColor` 테이블에 upsert). 태그 색상과 마찬가지로 저장 버튼 없이 선택 즉시 반영됨
- 변경한 색은 태스크 카드의 우선순위 표시(체크박스 테두리)에 바로 반영됨
- 태그 색상 · 우선순위 색상 설정은 사이드바 하단의 네이티브 `<details>` 접이식 "⚙ 설정" 메뉴 하나로 통합되어 있음 (기본 상태는 닫힘)

## 6. Phase 2 (계획 · 미구현) 📋

- 캘린더 뷰
- 완료 통계 / 생산성 리포트
- 자연어 입력 파싱 (예: "내일 3시 보고서" → 자동 날짜 인식)
- 다크모드 토글, 키보드 단축키
- 태그별 필터 UI

## 7. Phase 3: 주간보고 ✅ 구현 완료

### 7.1 후보 데이터 추출 로직 (`src/app/report/page.tsx`)
선택한 주간 범위(기본 이번 주 월~일, `?week=YYYY-MM-DD` 쿼리로 다른 주 조회 가능, 페이지 상단 "지난 주/다음 주" 링크로 이동) 기준으로 두 후보 목록을 자동 추출한다.

- **완료 업무 후보**: `completedAt`이 선택한 주간 범위 내인 **최상위** 태스크
- **완료된 서브태스크 후보**: `completedAt`이 선택한 주간 범위 내인 서브태스크(`parentId != null`)도 완료 후보 목록에 개별로 포함됨. 제목은 `상위 태스크 제목 › 서브태스크 제목` 형식으로 표시해서 어느 상위 소속인지 알 수 있게 하고, 기본적으로 체크되어 보고서에 바로 포함됨 (완료 후보와 같은 정렬·같은 기본 체크 규칙 적용, 상위 태스크 이름으로만 뭉뚱그려 보이던 문제 수정)
- **진행 중 / 미완료 업무 후보**: `completed=false` 이면서 아래 중 하나에 해당하는 최상위 태스크
  - `dueDate`가 선택한 주간 범위 내 (이번 주가 기한이었는데 못 끝낸 것)
  - `updatedAt`이 선택한 주간 범위 내 (이번 주에 생성되었거나 내용에 진전이 있었던 것 = "진행된 내용")
  - **서브태스크 중 하나라도 `completedAt`이 선택한 주간 범위 내인 경우** (상위 태스크 자체는 아직 안 끝났지만 하위 항목 하나를 이번 주에 끝낸 것도 "진행중"으로 잡음 — 이 서브태스크는 위 "완료된 서브태스크 후보"에도 별도로 뜨므로 진행중 후보의 상위 태스크와 완료 후보의 서브태스크 항목에 동시에 나타날 수 있음, 의도된 동작)

### 7.2 선택(Select) UI (`src/components/WeeklyReportBuilder.tsx`)
- 후보는 자동으로 뽑아 보여주되, **보고서에 실제로 포함할지는 사용자가 체크박스로 직접 선택**한다.
- 기본 체크 상태: **완료 후보는 전체 체크**, **진행/미완료 후보는 전체 체크 해제**로 시작한다 (자동으로 이것저것 다 올라가는 오탐 방지 — 사용자가 올릴 것만 직접 고른다).
- 체크 상태를 바꾸면 우측 마크다운 미리보기가 즉시 갱신된다 (클라이언트 상태, 저장되지 않음 — 새로고침하면 기본값으로 리셋).

### 7.3 출력 포맷 (`src/lib/weeklyReportTemplate.ts` — 렌더링 로직을 별도 함수로 분리, 회사 양식이 정해지면 이 함수만 교체)

```markdown
## 주간업무보고 (YYYY-MM-DD ~ YYYY-MM-DD)

### ✅ 금주 완료 업무
- [프로젝트명] 업무 제목 (완료일: MM/DD)

### 🔄 진행 중 / 미완료 업무
- [프로젝트명] 업무 제목 — 마감: MM/DD (마감일 없으면 "진행 중")
```

- 화면에 렌더링 + "복사" 버튼으로 클립보드 복사 (`navigator.clipboard.writeText`, 실패 시 조용히 무시하고 미리보기에서 수동 복사 가능)
- 회사 정식 양식은 나중에 별도로 전달 예정 → `weeklyReportTemplate.ts`의 `renderWeeklyReport()` 함수만 교체하면 되도록 로직과 포맷을 분리해서 구현함

## 8. 변경 관리 원칙 (중요)

- 이 문서는 프로젝트 요구사항의 **단일 기준(SSOT)** 이다.
- 새 기능·수정을 요청할 때는 가능하면 **몇 절을 어떻게 바꾸는지** 구체적으로 지정한다 (예: "7.2절을 이렇게 바꿔줘").
- 별다른 언급이 없는 한, 이미 구현되어 5절에 명시된 기능과 6~7절의 계획 범위는 **그대로 유지한 채 추가/구현만** 진행한다. 관련 없는 기능을 임의로 바꾸지 않는다.
- 기능을 변경하거나 없애야 할 때는 이 문서도 함께 갱신해서 항상 코드와 문서 상태가 일치하도록 한다.

## 9. 알려진 제한사항 / 엣지 케이스 (버그는 아니지만 인지하고 있어야 하는 동작)

- **매월 반복의 말일 오버플로**: `nextDueDate()`(`src/lib/actions/tasks.ts`)가 `Date.setMonth()`를 그대로 씀 — 예를 들어 마감일이 1/31인 매월 반복 태스크를 완료하면 다음 회차가 "2/28"이 아니라 자바스크립트 `Date`의 월 오버플로 규칙에 따라 **3/3**처럼 다음 달로 밀려서 계산될 수 있음. 월말 날짜로 매월 반복을 쓰는 사용자가 있다면 언젠가 고쳐야 할 항목
- **자동화 테스트 없음**: `package.json`에 `test` 스크립트 자체가 없다. 지금까지 모든 검증은 `npx tsc --noEmit` + `npm run lint` + 브라우저 수동 조작(+ 필요시 `better-sqlite3`로 DB 직접 조회)으로만 해왔음
- **로컬 SQLite라 `dev.db`/`.env`는 git에 없음**(`.gitignore`에서 제외): 새 환경(새 PC, 새 클론)에서는 `.env`에 `DATABASE_URL="file:./dev.db"` 직접 생성 + `npx prisma migrate dev` 또는 `npx prisma db push`로 스키마부터 새로 만들어야 실행됨. 기존 데이터는 옮겨지지 않음
- **`d`/`r` 다중 선택 단축키는 Shift는 막지 않음**: `Ctrl/Alt/Meta`가 눌려있으면 무시하지만 `Shift+d`/`Shift+r`은 그대로 동작함 (의도적인지 확인 안 된 사소한 틈)
- **서브태스크 위에 드롭해도 그 서브태스크 자신의 하위로는 못 들어감**: 드래그 앤 드롭 핸들러가 각 서브태스크 행이 아니라 최상위 태스크 `<li>` 전체에 걸려 있어서(이벤트 버블링), 어느 서브태스크 위에 놓아도 결국 "그 서브태스크가 속한 최상위 태스크"의 하위로 편입된다 — 5.5절의 "서브태스크는 1단계까지만" 제약이 드래그 앤 드롭에서도 자연히 유지되는 이유

## 10. 성능 · 실행 환경

기능은 그대로 두고 반응/저장/로드 속도만 최적화한 결과. 아래 항목은 화면에 보이는 동작을 바꾸지 않는다.
회차별 변경 이력과 측정값, 검토했으나 적용하지 않은 것들은 [`PERF.md`](PERF.md)에 정리되어 있다.

### 10.1 앱을 "쓸" 때는 개발 서버로 띄우지 않는다 (가장 큰 차이)

- `dev.cmd`(=`next dev`)는 **개발용**이다. 요청마다 라우트를 컴파일하고 최적화를 끈 React를 쓰기 때문에 같은 화면이라도 훨씬 느리다.
- 실사용은 **`run.cmd`**(=`npm run build && npm start`)로 띄운다. 이 PC 기준 서버 렌더링 `46ms → 14ms`(3.3배), 초기 HTML `35.4KB → 27.6KB`.
- 코드를 고친 뒤에는 `run.cmd`를 다시 실행해야 반영된다 (개발 서버와 달리 자동 반영 안 됨). 기능을 개발하는 중에는 그대로 `dev.cmd`를 쓰면 된다.

### 10.2 SQLite WAL 모드 (저장 속도)

- `src/lib/prisma.ts`가 부팅 시 `PRAGMA journal_mode = WAL`을 한 번 설정한다. 기본값(`delete`)은 커밋마다 저널 파일을 만들고 지우며 fsync를 두 번 한다.
- 이 PC 기준 단일 쓰기 지연 `1.42ms → 0.03ms` (약 48배). `journal_mode`는 DB 파일 헤더에 저장되는 영구 설정이라 한 번만 켜면 이후 모든 연결이 이어받는다.
- 부수 효과로 `dev.db-wal`, `dev.db-shm` 파일이 생긴다 (gitignore 처리됨). **DB를 다른 곳으로 복사할 때는 이 두 파일도 같이 옮기거나, 앱을 끈 상태에서 복사할 것.**

### 10.3 인덱스 (로드 속도)

- 마이그레이션 `20260908000049_add_indexes`. 그 전에는 Task/TaskTag에 인덱스가 하나도 없어서 모든 조회가 전체 스캔이었다.
- `Task(parentId, completed, dueDate)` — 4개 뷰 전부가 이 순서로 걸러서 하나로 커버됨 / `Task(parentId)` — 서브태스크 로딩 / `Task(projectId)` — 프로젝트 필터·자동정리 / `Task(completed, completedAt)` — 주간보고 / `TaskTag(tagId)` — 태그 자동정리·태그별 칸반 (복합 PK 선두가 `taskId`라 `tagId` 단독 조회는 인덱스를 못 탔음)
- 현재 데이터량(태스크 수십 건)에서는 체감 차이가 없다. 태스크가 수천 건으로 늘어날 때를 위한 것.

### 10.4 서버 왕복 줄이기

- **`revalidatePath("/")` → `refresh()`** (Next 16, `next/cache`): 전자는 이전에 방문한 다른 페이지까지 다시 불러오게 만든다. `refresh()`는 지금 보고 있는 라우트만 갱신한다. 덤으로 주간보고 화면에서 `n` 팝업으로 태스크를 추가해도 그 화면이 바로 갱신된다.
- **페이지 쿼리 병렬화**: 목록/주간보고 페이지가 프로젝트·태그·우선순위색·태스크를 순차로 `await` 하던 것을 `Promise.all` 하나로 묶었다.
- **한 액션 = 한 트랜잭션**: 완료 처리와 반복 일정 다음 회차 생성을 `$transaction`으로 묶어 커밋(=fsync)을 한 번만 한다.
- **자동정리 쿼리 축소**: `cleanupUnusedProjectAndTags`가 후보마다 `count`를 날리던 것(1+N 왕복)을 `groupBy` 두 번으로 바꿨다. 삭제도 한 번에 묶는다.

### 10.5 클라이언트

- **쓰지도 않는 웹폰트 제거**: `layout.tsx`가 Geist Sans/Mono를 불러오고 있었지만 `globals.css`의 `body { font-family: Arial, ... }`가 덮어써서 **한 번도 렌더에 쓰이지 않았다**. 걷어냄 — 화면은 그대로다.
- **`TaskItem`을 `memo`로 감쌈**: 태스크 하나를 선택하면 목록 전체가 다시 렌더되던 것을, 실제로 달라진 행만 렌더하도록 바꿈 (`TaskList`가 콜백을 `useCallback`으로 안정화).
- **칸반 그룹핑 1회 순회**: 컬럼마다 `tasks.filter`를 돌려 태스크×컬럼 만큼 비교하던 것을 한 번 순회하며 나눠 담는 방식으로 바꾸고 `useMemo`로 감쌌다.

### 10.6 측정값 요약 (이 PC 기준, 태스크 26건)

| 항목 | 전 | 후 |
|---|---|---|
| 페이지 서버 렌더 | 46.4ms (dev) | 14.0ms (prod) |
| 단일 DB 쓰기 | 1.42ms | 0.03ms |
| 태스크 추가 왕복(저장→화면반영) | — | 약 10ms |
| 다중 선택 N개 완료/삭제 | 서버 왕복 N회 | 1회 |
| 완료/삭제 클릭 반응 | 서버 응답 후 | 즉시(낙관적) |
| 초기 HTML | 35.4KB | 27.6KB |
| 웹폰트 요청 | Geist 2종 | 없음 |
