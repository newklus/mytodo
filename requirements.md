# MyTodo 요구사항 명세서

> 마지막 갱신: 2026-09-08 (5.2절 — 시작 페이지 설정을 고르면 쿠키 저장뿐 아니라 즉시 그 화면으로 이동하도록 수정(다른 페이지에 머문 채로는 설정이 반영 안 되던 버그). 5.2절 — 사이드바 설정에 "시작 페이지"(오늘/전체/캘린더) 추가, `/`를 파라미터 없이 열었을 때만 적용(쿠키 저장, 서버에서 바로 반영). 5.1.2절 — 일반 선택(클릭, 이전 선택 해제하고 단일 선택)과 다중 선택(조합키+클릭, 기본 Ctrl, 재배정 가능)을 구분, 호버 영역이 아이콘까지 포함하도록 확장. 5.1.1절 — 단축키 재배정 중 Enter(단독)도 취소로 처리하고 Enter 단독은 배정 불가하게 막음, 잠금 아이콘을 이모지에서 회색조 SVG로 교체. 5.8절 — 캘린더 "미등록 일정"도 "⚙ 설정"처럼 펼치면 아래로 스크롤·접으면 이전 위치로 스크롤 복원(`CollapsibleSection.tsx`). 5.8절 — 캘린더 "미등록 일정"을 펼치면 그리드/패널과 겹쳐 보이던 버그 수정: 그리드/패널 컨테이너가 `flex-1`로 "남은 공간 채우기"를 하려다 짜부라지던 원인을 없애고 내용물 그대로의 자연스러운 높이만 갖도록 함. 5.7.1절 — "⚙ 설정"을 펼칠 때 처음엔 스크롤이 안 따라가던 문제 수정(펼친 뒤 직접 스크롤 위치 계산해서 이동). 5.1절 — 태스크 메모를 목록에서 기본으로 펼쳐 보여주고 클릭으로 개별 접기/펼치기 가능, 기본값은 사이드바 설정에서 변경 가능. 5.1.2절 — 목록 위 선택 안내 바(단축키 힌트 + 선택 해제 버튼)를 없애고 "N개 선택됨"만 페이지 헤딩 옆으로 이동(단축키는 하단 바에 이미 있어 중복 제거). 5.1.1절 — 단축키 바에 🔒 잠금 토글 추가(기본 잠금, 해제해야 재배정 가능, 잠금 상태는 저장 안 됨). 5.7.1절 — 사이드바에 내부 스크롤 추가하고 단축키 바를 사이드바 폭만큼 띄워 겹침 방지. 5.1.1절 — 화면 하단 단축키 바 신설, 클릭해서 단축키 재배정 가능(`localStorage` 저장). 5.7.1절 신설 — 사이드바·캘린더 그리드/패널 크기를 드래그로 조절, 다음 접속 시 로드. 5.8절 — 캘린더 이전달/오늘/다음달 버튼을 월 표시 옆으로 이동. 5.1/5.1.2절 — 태스크 편집은 먼저 선택된 항목의 본문을 다시 클릭해야 열리도록 변경, 아이콘이 아니어도 본문 클릭(호버 시 우선순위색 테두리 표시)으로 선택 가능. 5.2절 — 완료 뷰를 제외한 모든 뷰의 정렬을 우선순위 → 생성일 순으로 통일(마감일 정렬 제거), 완료 뷰는 최신 완료일 우선(우선순위 무관)으로 변경. 5.2절 — 완료 뷰를 최근 15일 이내 완료 항목만 표시하도록 변경(그 이전 항목은 DB엔 남지만 화면에서 제외). 10.7절 신설 — 부팅 시 일일 자동 백업(최신 7개 유지, `backups/`, 이관 아닌 순수 복제). 5.1.2/5.1.3절 — `Shift+d`(완료 취소)·`Esc`(선택 해제)·`u`(실행취소) 단축키 추가. 5.1.3절 신설 — 완료/삭제 1단계 실행취소(Undo) 토스트 추가. 5.8절 — 캘린더 아래 "미등록 일정"(마감일 없는 태스크) 접이식 섹션 추가. 5.1절 — `@`/`#` 멘션을 Enter 한 번에 전부 확정하도록 `resolveEnter`로 통합(활성 토큰 하나만 확정되고 나머지는 Enter를 더 눌러야 했던 문제 수정). 4·5.1절 — 새 태스크 기본 우선순위 P4→P3. 5.8절 — 캘린더 칸 더블클릭 시 그 날짜를 마감일로 미리 채운 새 할일 팝업. 5.7절 — "⚙ 설정" 메뉴를 사이드바 하단에서 상단으로 이동(펼치면 화면 밖으로 넘치던 문제 수정). 5.1.2절 — 다중 선택 바에 "선택 해제" 버튼 추가. 5.8절 — 캘린더 뷰(`/calendar`) 신규 구현: 월간 그리드 + 사이드 패널, 6절에서 이동. 9절 — `d`/`r` 단축키에서 Shift 조합 차단(추후 별도 기능 배정 예정). 5.6절 — 매월 반복의 말일 오버플로 버그 수정. 5.1절 — `@`/`#` 자동완성 드롭다운에서 Tab/Enter 동작 통일: 드롭다운이 떠 있으면 Enter도 Tab처럼 강조된 항목을 선택. 부분 입력 시 새 항목이 잘못 생성되던 문제 수정)
> 이 문서는 프로젝트 요구사항의 **단일 기준(SSOT)** 입니다. 8절의 변경 관리 원칙을 반드시 지켜주세요.

## 1. 프로젝트 배경

- 클로드(Claude Code) 사용법 학습 + 효율적인 프롬프트 작성법을 익히기 위한 실습 프로젝트 (`BG.md` 참고)
- 실습 소재로 **회사 업무에 실제로 쓸 수 있는 개인용 TODO 앱**을 만든다.

## 2. 목적 및 범위

- **용도**: 개인용 (본인 업무 관리). 팀 협업 기능(공유, 권한, 배정 등)은 범위 밖.
- **목표 수준**: 학습용 장난감이 아니라 실제 회사 업무에 쓸 수 있는 수준의 완성도.
- **벤치마크**: Todoist, Things3, TickTick, Any.do 등 개인 생산성 툴의 핵심 기능(우선순위, 오늘/예정 뷰, 반복 일정)을 차용하고, 여기에 회사 업무 특화 기능인 **주간보고 자동화**를 추가한다.

## 3. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js 16 (App Router, TypeScript, `src/` 디렉토리) |
| 스타일 | Tailwind CSS 4 |
| DB | SQLite (`dev.db`, 프로젝트 루트) |
| ORM | Prisma **7.10.0 고정** (8.x는 아직 RC라 사용하지 않음) + `@prisma/adapter-better-sqlite3` 드라이버 어댑터 필수 |
| 데이터 변경 | Next.js Server Actions만 사용 (별도 REST API 없음) |
| 배포(예정) | Vercel |

## 4. 데이터 모델 (`prisma/schema.prisma`)

- **Project**: `id, name, color?, createdAt` — `tasks[]`
- **Tag**: `id, name(unique), color?` — `tasks: TaskTag[]`
- **Task**: `id, title, description?, dueDate?, priority(1~4, 기본 3), completed, completedAt?, recurrence?("DAILY"|"WEEKLY"|"MONTHLY"), order, projectId?, parentId?(자기참조)` — `subtasks[], tags: TaskTag[]`
- **TaskTag**: Task-Tag 조인 테이블 (`taskId + tagId` 복합키)
- **PriorityColor**: `priority(1~4, PK), color` — 우선순위별 커스텀 색상 (레코드 없는 우선순위는 `DEFAULT_PRIORITY_COLORS` 기본값 사용)

## 5. 구현 완료 기능 (Phase 1 · MVP) ✅

### 5.1 태스크 CRUD
- 생성: 제목(필수) / 마감일 / 우선순위(P1~P4, **기본값 P3 · 보통**) / 프로젝트 / 반복 규칙 / 태그(쉼표 구분)
- **퀵애드(inline 폼 + `n` 팝업)의 제목 입력창은 `@프로젝트`, `#태그` 인라인 멘션을 지원** (`SmartTitleInput` 컴포넌트, 태스크 편집 화면은 기존 드롭다운 방식 그대로 유지 — 여기는 적용 안 함)
  - `@`나 `#`을 입력하면 그 순간부터 뒤에 붙는 글자로 실시간 필터링된 드롭다운이 뜸 (기존 프로젝트/태그 중 이름에 포함되는 것 최대 6개)
  - 정확히 일치하는 기존 항목이 없으면 목록 맨 아래 "+ 새로 만들기: {입력값}" 옵션이 추가됨. 태그는 실제 태스크 생성 시 기존 `connectOrCreate` 흐름으로 자동 등록되고, 프로젝트는 선택 즉시 `findOrCreateProject` 서버 액션으로 미리 생성(또는 기존 항목 매칭)해서 id를 받아옴 — 색은 항상 랜덤 배정
  - 방향키(↑↓)로 후보 이동(강조는 다크/라이트 모두 대비가 뚜렷한 반전 배경 스타일). **드롭다운이 떠 있는 동안은 Tab과 Enter가 동일하게 동작**한다 — 둘 다 강조된 항목을 그대로 선택한다(부분 입력이어도 OK). Esc는 드롭다운만 닫기(팝업 자체는 안 닫힘)
  - **Enter 한 번으로 텍스트에 남아있는 `@단어`/`#단어`를 전부 한꺼번에 칩으로 확정**한다 (`resolveEnter`): 지금 드롭다운이 떠 있는(=아직 스페이스를 안 친) 활성 토큰은 강조된 항목으로(Tab과 동일하게, 부분 입력이어도 OK), 이미 스페이스로 닫힌 나머지 토큰들은 대소문자만 무시한 완전일치로 — 이 둘을 한 번에 처리한다. 예를 들어 `제목 @프로젝트 #태그`까지 한 번에 타이핑하고 Enter 한 번만 누르면 프로젝트·태그가 동시에 확정되고 제목만 깨끗하게 남으며, 이 Enter는 제출로 이어지지 않는다. 확정할 게 더는 없을 때(=제목이 이미 깨끗할 때) 눌러야 그다음 Enter가 실제로 폼을 제출함
  - (과거엔 활성 토큰과 나머지 텍스트를 서로 다른 함수가 각자 따로 처리해서, 드롭다운이 떠 있는 상태로 Enter를 누르면 그 토큰 하나만 확정되고 나머지는 Enter를 한 번 더 눌러야 했던 문제, 그리고 그 이전엔 부분 입력한 채 Enter를 누르면 완전일치만 인정돼 별개의 새 항목이 생기던 문제가 있었음 — `resolveEnter`로 통합해서 둘 다 해결)
  - `@`/`#`는 **앞이 공백이거나 문장 시작일 때만** 멘션으로 인식됨(정규식 `(^|\s)[@#]\S+`) — 예: `hello@world` 처럼 앞에 공백이 없으면 멘션으로 파싱되지 않음(이메일 등 오인식 방지). 클라이언트(`parseAllMentions`)와 서버 안전망(`extractMentions`)이 동일한 규칙을 씀
  - 선택하면 `@검색어`/`#검색어` 텍스트는 입력창에서 바로 사라지고, 그 옆에 칩(프로젝트 칩 1개 + 태그 칩 여러 개, × 버튼으로 제거 가능)으로 표시됨 — **입력값 결과(제목)에는 @/# 텍스트가 절대 남지 않음**
  - 새로 만드는 프로젝트·태그 이름은 **입력한 대소문자 그대로** 등록됨 (검색/일치 비교만 대소문자 무시, 실제 생성 라벨은 원문 그대로 사용)
  - 새로 생성되는 프로젝트·태그의 색상은 완전 임의 색이 아니라 **정해진 7색 팔레트 중 무작위 하나** (`PALETTE`, `src/lib/colors.ts`: 빨강 `#ef4444`/주황 `#f97316`/노랑 `#eab308`/초록 `#22c55e`/파랑 `#3b82f6`/보라 `#8b5cf6`/분홍 `#ec4899`)
  - 드롭다운 항목·칩에 표시되는 이름이 24자보다 길면 중간을 `…`으로 생략 (`truncateMiddle`, `src/lib/truncate.ts`)
  - `@`로 새 프로젝트를 만드는 동안은 서버 왕복(생성/조회) 때문에 아주 짧게 대기 시간이 생기는데, 이 사이에 바로 제출하면 프로젝트 연결 없이 태스크가 생성될 수 있어서 **생성 중에는 "추가" 버튼이 비활성화**됨("프로젝트 생성 중…" 표시). `onPendingChange` 콜백으로 부모 폼에 진행 상태를 알려줌
  - **서버 쪽 안전망**: 드롭다운으로 확정하지 않고 `@단어`/`#단어`가 제목에 그대로 남은 채 제출돼도, `createTask` 서버 액션의 `extractMentions()`가 제목에서 그 토큰들을 다시 뽑아내 프로젝트/태그로 연결하고 제목에서는 지움 (`src/lib/actions/tasks.ts`). 클라이언트 드롭다운을 전혀 안 써도 `@`/`#` 문법 자체는 항상 동작함
  - **`+N` 마감일 단축입력**: 제목에 `+숫자`를 쓰면(앞뒤가 공백/문장 끝) N일 뒤 날짜가 마감일로 자동 설정되고 `+N` 텍스트는 제목에서 지워짐 (`+1`=내일, `+3`=3일 뒤). `src/lib/plusDate.ts`의 `parsePlusDate()` 순수함수를 클라이언트(`SmartTitleInput`, 실시간으로 마감일 입력창에 미리 채워줌)와 서버(`createTask`의 안전망, 명시적으로 마감일을 고른 게 없을 때만 폴백) 양쪽에서 동일하게 사용
    - `+N`도 `@`/`#`와 동일하게 **앞뒤가 공백/문장 경계일 때만** 인식되고(정규식에 `/g` 없이 **첫 번째 매치 하나만** 치환), 날짜 선택기(`<input type="date">`)에 사용자가 이미 값을 직접 골랐으면 `+N`이 있어도 **절대 덮어쓰지 않음** (명시적 선택이 항상 우선)
- 수정: **먼저 선택된 태스크만 편집 폼이 열린다** — 선택되지 않은 태스크의 본문(제목·메타 정보 영역)을 클릭하면 그 클릭은 선택만 하고(5.1.2 참고), 이미 선택된 태스크의 본문을 다시 클릭해야 인라인 편집 폼(제목/설명(메모)/마감일/우선순위/프로젝트/반복/태그)이 열린다
- **저장 버튼 없음 — 포커스가 폼 밖으로 나가면 자동 저장됨** (`onBlur`, `relatedTarget`이 폼 안이면 무시). 값이 원래 태스크와 동일하면 저장 호출 없이 그냥 보기 화면으로 돌아가고, 하나라도 바뀌었으면 자동으로 `updateTask` 호출 후 보기 화면으로 전환. "취소" 버튼은 남겨둬서 명시적으로 변경을 버리고 닫을 수 있음
- **한 번에 하나의 태스크만 편집 모드일 수 있음**: 편집 중인 태스크 A의 제목을 클릭해 편집 폼을 연 채로 다른 태스크 B의 제목을 클릭하면, A가 먼저 위 자동저장 로직을 거쳐 보기 화면으로 닫힌 뒤 B가 편집 모드로 열림. 태스크 제목/서브태스크 제목 모두 클릭 시 `document.activeElement.blur()`를 먼저 호출해서 이전 폼의 `onBlur`가 확실히 트리거되도록 하고, 편집 폼 제목 입력창에는 `autoFocus`를 둬서 다음 클릭 때 blur 대상이 정확히 잡히게 함 (`TaskItem.tsx`)
- 메모(설명) 필드는 여러 줄 입력 가능한 textarea (`rows=3`). 메모가 있으면 목록에서 📝 아이콘과 함께 **메모 내용이 기본적으로 그대로 펼쳐져 보인다**(제목·메타 정보 줄 아래 별도 줄). 📝 아이콘이나 펼쳐진 메모 텍스트를 클릭하면 접혔다 펼쳐졌다 토글된다(태스크마다 독립적, 새로고침하면 다시 기본값으로). 기본으로 펼칠지 접을지는 사이드바 "⚙ 설정" 안 "메모 표시" 체크박스로 바꿀 수 있고(기본은 펼침, `localStorage`의 `mytodo:memoDefaultExpanded`), 여기서 바꾼 기본값은 그 이후 렌더링되는 태스크부터 적용된다(이미 개별적으로 접거나 편 태스크를 소급 변경하지는 않음)
- **서브태스크에는 메모 필드를 두지 않음** (의도적 결정): 서브태스크는 빠른 체크리스트 용도로 가볍게 유지하고, 자세한 내용이 필요하면 메인 태스크의 메모를 쓰거나 별도 태스크로 승격시키는 쪽으로 유도

### 5.1.2 다중 선택 & 완료/삭제 (최상위 태스크만, `src/components/TaskList.tsx`)
- 행 맨 앞의 원 아이콘은 더 이상 완료 토글이 아니라 **선택 체크박스**. 테두리는 항상 우선순위 색, 선택되면 그 **우선순위 색으로 배경까지 채워짐** (검정 아님)
- **일반 선택 vs 다중 선택**(`src/lib/shortcuts.ts`의 `multiSelectModifier`, 기본 `Ctrl`):
  - **조합키 없이 클릭(일반 선택)**: 아이콘이든 본문(제목·메타 정보)이든 클릭하면 **이전 선택은 모두 해제되고 이 태스크만 선택**된다(탐색기류 단일 선택과 동일). 단, **이미 이 태스크 하나만 선택된 상태에서 본문을 다시 클릭하면** 선택을 유지한 채 **편집 모드**로 들어간다(위 5.1 "수정" 참고) — 아이콘을 다시 클릭하는 건 항상 선택 동작이라 편집으로 이어지지 않음
  - **조합키(기본 Ctrl)를 누른 채 클릭(다중 선택)**: 아이콘/본문 어디를 클릭해도 기존 선택은 유지한 채 그 태스크만 토글(추가/제거)된다. 편집으로는 절대 이어지지 않음
  - **아이콘이 아니어도 선택 가능**: 행에서 아이콘+본문을 함께 감싸는 영역에 마우스를 올리면 **우선순위 색 테두리**가 나타나 클릭 가능함을 알려준다(아이콘 위에 올려도 동일하게 나타남 — 호버 영역이 아이콘을 포함)
  - 다중 선택 조합키는 화면 하단 단축키 바에서 "Ctrl+클릭 다중선택"처럼 표시되고, 다른 단축키와 마찬가지로 잠금 해제 후 클릭 → 새 수정자 키(Ctrl/Alt/Shift/Meta 중 하나) 입력으로 재배정할 수 있다(`getMultiSelectModifier`/`setMultiSelectModifier`, `localStorage`의 `mytodo:multiSelectModifier`)
- 완료/취소·삭제는 행 오른쪽의 **"완료"/"삭제" 버튼**으로 실행 (기존에 삭제 버튼이 있던 자리에 나란히 배치). 완료 버튼은 이미 완료된 태스크면 "완료 취소"로 라벨이 바뀌어 다시 미완료로 되돌릴 수 있음. 둘 다 클릭 시 확인창 1회
  - 미완료 태스크에서는 마우스를 올려야 나타남(호버 리빌), **완료된 태스크에서는 항상 보임** — 완료 뷰에서 "완료 취소"를 찾기 어렵다는 피드백을 받아 완료된 항목은 상시 노출로 바꿈
- **선택 개수는 목록 위가 아니라 페이지 헤딩(전체/오늘/예정/완료) 바로 옆에 "N개 선택됨"으로 표시**된다(`SelectionCountBadge.tsx` + `src/lib/selectionCount.ts`, `window` 커스텀 이벤트로 `TaskList`가 헤딩에 개수를 전달 — `undoBus`/`quickadd:open`과 동일한 관례). 예전엔 목록 위에 별도 안내 바("N개 선택됨 · d 완료 · ... · 선택 해제" + 버튼)가 있었지만, 단축키 자체는 5.1.1절의 화면 하단 단축키 바에 항상 떠 있어 중복이라 걷어냄 — 선택 해제는 `Esc`(또는 재배정한 키)로만 가능하고 별도 버튼은 없음
- **`d`/`Shift+d`/`r` 단축키**: 선택된 태스크가 하나 이상 있을 때만 동작 (입력 필드에 포커스 없을 때, `d`/`r`은 수정자 키 없이). `d`=선택된 항목 전체 완료 처리, `Shift+d`=선택된 항목 전체 완료 취소(미완료로), `r`=선택된 항목 전체 삭제. 실행 전 확인창 1회 ("선택한 N개 작업을 완료 처리/완료 취소/삭제할까요?"), 확인 즉시 선택 해제. `Shift+r`은 마땅한 반대 동작이 없어 계속 비워둠
  - N개를 골라도 서버 호출은 **한 번**이다 (`completeTasks`/`deleteTasks` 일괄 액션, 10절 참고). 선택 개수와 무관하게 DB 왕복 1회 · 화면 갱신 1회로 끝난다
- **`Esc`**: 선택된 태스크가 있을 때 누르면 선택 해제(확인창 없음)
- **완료/완료취소/삭제한 행은 서버 응답을 기다리지 않고 즉시 목록에서 사라진다** (`useOptimistic`). 완료 뷰에서는 완료 취소한 것이, 나머지 뷰에서는 완료 처리한 것이 그 뷰의 조건을 벗어나므로 어느 쪽이든 결과는 같다. 서버 처리가 끝나면 실제 목록으로 자연히 대체되고, 실패하면 원래대로 되돌아온다. 서브태스크의 완료 토글·삭제도 동일하게 즉시 반영됨
- 서브태스크의 원 아이콘은 그대로 완료 토글 (다중 선택 대상 아님)

### 5.1.3 실행취소(Undo) — 1단계 (`src/lib/undoBus.ts`, `src/components/UndoToast.tsx`)
- **완료/완료취소/삭제(단건·일괄, 최상위·서브태스크 전부)** 직후 화면 하단에 "OO함 · 실행취소" 토스트가 뜬다. 8초 안에 "실행취소"를 누르면 방금 한 동작을 되돌린다. 시간이 지나거나 ✕로 닫으면 사라짐
- **1단계만 지원**: 되돌릴 수 있는 건 항상 "가장 최근 동작 하나"뿐이다. 새 완료/삭제를 하면 이전 토스트는 그대로 대체된다(쌓이지 않음). 되돌리기(redo)나 그 이상 단계는 범위 밖 — 필요해지면 나중에 늘리기로 함(대화 중 논의)
- 기존 `confirm()` 확인창은 그대로 유지된다 (실행취소는 "확인창 대신"이 아니라 "확인하고 실행한 뒤에도 한 번 더 되돌릴 기회"로 추가된 것)
- **적용 범위**: `toggleTaskComplete`/`completeTasks`/`deleteTask`/`deleteTasks` — 이 네 서버 액션만 되돌릴 수 있다. 수정(`updateTask`)·이름변경·서브태스크 이동·생성은 대상이 아님(생성은 그냥 다시 지우면 되고, 수정/이동은 되돌리려면 필드 단위 스냅샷이 필요해 1단계 범위에서 제외)
- **복원 방식**: 완료 취소는 토글 전 `completed`/`completedAt`으로 되돌리는 것뿐이라 간단하다. 삭제는 **하드 삭제**라 되돌리려면 지우기 전에 대상(+cascade로 같이 지워질 서브태스크, 태그 연결)을 전부 스냅샷으로 남겨뒀다가 **원래 id 그대로** 다시 만든다. 이 삭제가 5.3/5.4절의 자동정리로 프로젝트·태그까지 같이 지웠을 수 있어서, 그 원본 행도 스냅샷에 같이 담아 되살릴 때 먼저 복원(upsert)한다
- **완료 처리 되돌리기는 반복 일정의 다음 회차도 같이 정리한다**: 반복 태스크를 완료해서 다음 회차가 자동 생성됐었다면, 되돌릴 때 그 다음 회차도 함께 삭제해서 원래 상태(원본 1건, 미완료)로 정확히 복원한다
- DB에 변경 이력을 남기지 않는 방식이라(브라우저 메모리에만 잠깐 들고 있음) 새로고침하면 그 시점의 "되돌리기"는 사라진다 — 딱 "방금 실수"를 잡는 용도
- best-effort: 되돌리는 사이 그 태스크가 다른 조작으로 이미 사라졌거나 충돌 나면 조용히 포기한다(에러 노출 안 함)
- **`u` 단축키**: 토스트가 떠 있을 때(=되돌릴 게 있을 때)만 동작, 클릭 대신 키보드로 바로 실행취소

### 5.1.1 키보드 단축키
- **화면 맨 아래 단축키 바**(`ShortcutBar.tsx`, 모든 페이지 레이아웃에 항상 표시, 왼쪽 사이드바 폭만큼 띄워서 사이드바와 안 겹침)에 현재 단축키 목록이 칩으로 보인다
  - **기본은 잠금 상태**(자물쇠 아이콘, 회색조 SVG — 페이지를 새로 열 때마다 항상 잠금으로 시작, 저장 안 됨): 이 상태에서는 칩이 비활성화되어 클릭해도 아무 일도 안 일어난다. 맨 앞의 자물쇠 아이콘을 누르면 풀리고, 그 상태에서만 칩을 눌러 재배정할 수 있다(다시 누르면 재잠금)
  - 잠금 해제 후 **칩을 클릭하면 다음 키 입력으로 그 단축키를 바로 재배정**할 수 있다(`src/lib/shortcuts.ts`). 재배정 중엔 "키 입력…"으로 표시되고 **Esc 또는 Enter(단독)로 취소** 가능. 이미 다른 동작에 쓰이는 키를 고르면 확인창으로 한 번 경고
  - **Enter 단독은 단축키로 지정할 수 없다** — 폼 제출 등 곳곳에서 이미 쓰이는 키라, 재배정 중 Enter를 누르면 배정 대신 Esc처럼 취소로 처리된다(Shift 등 다른 키와 조합한 Enter는 배정 가능)
  - 재배정 결과는 `localStorage`(`mytodo:shortcuts`)에 저장되어 다음 접속 때도 유지된다(잠금 상태 자체는 저장 안 되고 항상 잠금으로 리셋). 아래 `n`/`d`/`Shift+d`/`r`/`Esc`(선택 해제)/`u` 여섯 개가 재배정 대상이다(기본값 기준으로 이 문서에서는 계속 기본 키로 표기함)
  - **다중 선택 조합키**("Ctrl+클릭 다중선택" 칩)도 같은 잠금/재배정 방식을 따르지만, 키 하나가 아니라 **Ctrl/Alt/Shift/Meta 중 하나의 수정자**를 고르는 것이라 별도로 다룬다 — 5.1.2절 참고
- **`n`**: 어디서든(입력 필드에 포커스가 없을 때) 누르면 새 할일 팝업이 뜸 (`QuickAddModal` 컴포넌트, 모든 페이지에 마운트됨). 제목 입력창에 자동 포커스
- **`Enter`**: 새 할일 팝업과 태스크 인라인 편집 폼의 제목/태그 등 한 줄 입력 필드에서 저장/추가 실행 — 폼에 submit 버튼이 있어서 브라우저 기본 동작으로 되는 것이라 별도 JS 처리 없음
- **`Ctrl+Enter`** (Mac은 `Cmd+Enter`): 태스크 인라인 편집 폼에서 어느 필드에 있든 저장 — 특히 메모(textarea)에서는 일반 Enter가 줄바꿈으로 쓰이기 때문에 이 단축키가 필요함. (새 할일 팝업에는 메모 필드가 없어서 이 단축키를 안 둠, 그냥 Enter로 충분)
- **`Esc`**: 새 할일 팝업 닫기 (편집 폼은 기존처럼 취소 버튼으로 닫음). 팝업 바깥의 반투명 배경을 클릭해도 동일하게 닫힘(`QuickAddModal`의 오버레이 `onMouseDown`, 이벤트 타깃이 오버레이 자신일 때만 — 팝업 내부 클릭은 안 닫힘)
- **인라인 폼(`QuickAddForm`)과 팝업(`QuickAddModal`)의 입력 상태 초기화 방식이 다름**: `QuickAddForm`은 태스크 추가 후에도 화면에 계속 남아있는 폼이라 `SmartTitleInput`에 `key={resetKey}`를 주고 제출할 때마다 `resetKey`를 증가시켜 **컴포넌트를 통째로 리마운트**해서 칩/텍스트 상태를 지운다. `QuickAddModal`은 닫힐 때 `if (!open) return null`로 아예 언마운트되므로 다음에 열 때 자연히 빈 상태로 새로 마운트됨(별도 리셋 로직 불필요)
- **`d`/`r`**: 다중 선택된 태스크 일괄 완료/삭제 (5.1.2 참고, 확인창 1회)
- 삭제: 브라우저 confirm 확인 후 삭제

### 5.2 뷰 / 필터
- 사이드바 표시 순서: **전체 → 오늘 → 예정 → 완료**
- **정렬 공통 규칙(완료 뷰 제외)**: **우선순위 오름차순 → 그다음 생성일 오름차순(먼저 만든 게 위)**. 마감일은 정렬 기준에서 빠졌다(예전엔 전체 뷰만 마감일순이었지만, 화면에 계속 보이는 이상 마감일 없는 태스크가 묻힐 걱정이 없어 모든 뷰를 우선순위 기준으로 통일)
- **전체**: `completed=false` (마감일 무관)
- **오늘**: `completed=false AND dueDate <= 오늘 23:59:59` (마감일 없는 태스크는 표시 안 됨)
- **예정**: `completed=false AND dueDate > 오늘 23:59:59`
- **완료**: `completed=true AND completedAt >= 15일 전 00:00`. **정렬은 예외적으로 최신 완료일 우선(내림차순), 우선순위는 무관** — "방금 끝낸 일이 위로 오면 좋겠다"는 완료 뷰 특유의 용도에 맞춤. 15일보다 오래된 완료 항목은 화면에서만 제외되고 DB에는 그대로 남음(5.1.3절 완료 취소 되돌리기 등 참조 로직에 영향 없음)
- 위 4개 뷰 모두 서브태스크(`parentId != null`)는 목록에서 제외하고, 프로젝트 사이드바에서 프로젝트별 추가 필터 가능
- **`?view` 파라미터 없이 `/`에 진입했을 때(=앱을 새로 켰을 때)의 기본 화면은 사이드바 "⚙ 설정"의 "시작 페이지"에서 오늘/전체/캘린더 중 고를 수 있다**(기본값 오늘). `src/lib/startPage.ts` + 쿠키(`mytodo_start_page`, 1년 유지)에 저장되어 서버가 첫 렌더에서 바로 반영한다(localStorage와 달리 깜빡임 없음) — "캘린더"를 고르면 `/`가 `/calendar`로 서버 리다이렉트된다. 사이드바의 "전체"/"오늘" 링크 등 **view를 명시하는 모든 이동은 이 설정과 무관하게 항상 그 화면으로 간다** — 이 설정은 오직 파라미터 없는 진입에만 적용됨
  - **드롭다운에서 고르는 즉시 그 화면으로 바로 이동한다**(`router.push`, 쿠키 저장과 별개로). 이동 없이 쿠키만 바꾸면, 이미 다른 페이지(예: 캘린더)에 있는 상태에서는 그 페이지를 새로고침해도 반영될 방법이 없어서(캘린더 페이지엔 이 쿠키를 확인하는 로직이 없음) "설정을 바꿔도 안 바뀐다"처럼 보이는 문제가 있었음 — 실제로 겪은 버그

### 5.2.1 레이아웃: 목록 / 칸반 3종 (`?layout=list|priority|project|tag`, 기본 `list`)
- 상단에 "목록 / 우선순위별 / 프로젝트별 / 태그별" 토글 (헤딩 오른쪽)
- **우선순위별**: P1~P4 4개 컬럼
- **프로젝트별**: 프로젝트마다 컬럼 + "프로젝트 없음" 컬럼
- **태그별**: 태그마다 컬럼 + "태그 없음" 컬럼 — 태스크에 태그가 여러 개면 해당하는 모든 컬럼에 카드가 중복으로 나타남 (Notion 스타일 다중 그룹 표시)
- 위 3개 칸반 모두 현재 뷰·프로젝트 필터가 그대로 적용된 상태에서 재배열만 하는 것 (`src/components/TaskList.tsx`). 각 컬럼 안의 카드는 목록 뷰와 동일한 `TaskItem` 컴포넌트를 그대로 재사용 — 다중 선택/완료·삭제/서브태스크 드래그 앤 드롭 전부 컬럼을 넘나들며 동일하게 동작

### 5.3 프로젝트
- 사이드바에서 생성, 색상 랜덤 자동 배정
- 태스크에 프로젝트 배정 가능 (미배정 허용)
- **수동 삭제 버튼 없음 — 참조하는 태스크가 하나도 없어지면 자동 삭제됨**: 태스크 수정/삭제, 서브태스크를 다른 부모로 이동시킬 때 이전 `projectId`를 기준으로 `prisma.task.count({ where: { projectId } })`가 0이면 그 프로젝트를 자동 삭제 (`cleanupUnusedProjectAndTags`, `src/lib/cleanupUnused.ts`). 완료/미완료 상관없이 태스크가 하나라도 참조 중이면 삭제되지 않음

### 5.4 태그
- 쉼표로 여러 개 입력 → 없으면 자동 생성 후 연결, 새로 생성될 때 프로젝트처럼 랜덤 색상 자동 배정
- 태스크 카드에 색상 점 + `#태그` pill로 표시
- 사이드바 "⚙ 설정"(접이식, 기본 닫힘) 안 "태그 색상" 섹션에서 전체 태그 목록을 색상 점과 함께 보여주고, 점을 클릭하면 네이티브 색상 피커로 색을 바로 바꿀 수 있음 (`TagColorPicker` 컴포넌트, `updateTagColor` 서버 액션). **저장 버튼·확인창 없이 색을 고르는 즉시(`onChange`) 바로 반영됨** (네이티브 `<input type="color">`는 팔레트를 최종 확정할 때만 `onChange`가 발생하므로 드래그 중 매번 서버 호출이 일어나진 않음)
- 태그별로 묶어서 보는 건 5.2.1의 "태그별" 칸반으로 가능 (전통적인 필터 드롭다운은 아직 없음)
- **수동 삭제 버튼 없음 — 참조하는 태스크가 하나도 없어지면 자동 삭제됨**: 프로젝트와 동일한 방식으로, 태스크 수정/삭제 시 이전에 달려 있던 태그들 각각에 대해 `prisma.taskTag.count({ where: { tagId } })`가 0이면 그 태그를 자동 삭제 (완료/미완료 태스크 어느 쪽에서도 더 이상 안 쓰일 때만)

### 5.5 서브태스크
- 태스크당 **1단계** 체크리스트 (서브태스크의 서브태스크는 UI 미지원 — 스키마는 무제한 자기참조 지원)
- 개별 완료 토글 / 삭제, 자체 마감일·프로젝트·태그 없음(부모의 `projectId`만 상속)
- 부모 완료 시 서브태스크는 자동完료 처리되지 않음 (독립적으로 남음)
- 제목 클릭 시 인라인 텍스트 수정 가능 (`renameTask` 서버 액션, Enter로 저장·Escape로 취소)
- **드래그 앤 드롭으로 다른 상위 태스크로 이동 가능**: 행 맨 앞의 `⠿` 드래그 핸들 아이콘만 드래그 소스로 동작 (행 전체를 드래그 대상으로 하면 텍스트 클릭/수정과 충돌해서 핸들로 분리함). 다른 (최상위) 태스크에 드롭하면 그 태스크의 하위로 옮겨짐 (`moveSubtask` 서버 액션). 이동 시 `projectId`는 새 부모 태스크 것으로 다시 상속됨. 네이티브 HTML5 Drag and Drop API 사용, 별도 라이브러리 없음

### 5.6 반복 일정
- 옵션: 없음 / 매일 / 매주 / 매월
- 마감일이 있는 반복 태스크를 완료 처리하면: **원본은 완료 상태로 보존**(완료 이력 유지) + **다음 회차를 새 태스크로 자동 생성** (제목/설명/우선순위/프로젝트/반복 규칙만 복사, 태그·서브태스크는 복사 안 함)
- 마감일 없는 반복 태스크는 완료해도 다음 회차를 만들지 않음
- **매월 반복은 대상 월의 말일로 자동 클램프됨**: 마감일이 1/31인 매월 반복을 완료하면 다음 회차는 2/28(윤년이면 2/29)이 된다 (`nextDueDate()`, `src/lib/actions/tasks.ts`). 대상 월에 원래 day가 존재하면 그대로 유지되고(예: 3/15 → 4/15), 없을 때만 그 달의 마지막 날로 줄어든다

### 5.7 우선순위 색상
- P1~P4 기본 색상: 빨강/주황/파랑/회색 (`DEFAULT_PRIORITY_COLORS`, `src/lib/priorityColors.ts`)
- 사이드바 "⚙ 설정"(접이식, 기본 닫힘) 안 "우선순위 색상" 섹션에서 P1~P4 각각 네이티브 색상 피커로 커스텀 가능 (`PriorityColorPicker` 컴포넌트, `updatePriorityColor` 서버 액션 → `PriorityColor` 테이블에 upsert). 태그 색상과 마찬가지로 저장 버튼 없이 선택 즉시 반영됨
- 변경한 색은 태스크 카드의 우선순위 표시(체크박스 테두리)에 바로 반영됨
- 태그 색상 · 우선순위 색상 설정은 사이드바의 "프로젝트" 목록 바로 아래에 있는 네이티브 `<details>` 접이식 "⚙ 설정" 메뉴 하나로 통합되어 있음 (기본 상태는 닫힘, 펼치면 아래로 펼쳐짐) — 예전엔 사이드바 맨 아래(`mt-auto`)에 있어서 펼치면 화면 밖으로 넘어가는 문제가 있어 위치를 옮김

### 5.7.1 레이아웃 크기 조절
- **사이드바**(`Sidebar.tsx`)와 **캘린더의 그리드/사이드 패널 경계**(`CalendarSplit.tsx`)는 오른쪽 가장자리를 드래그해서 너비를 조절할 수 있다. 각각 `localStorage`(`mytodo:sidebarWidth`, `mytodo:calendarGridPercent`)에 저장되어 다음 접속 때도 그대로 로드됨. 사이드바는 180~400px, 캘린더 분할은 30~80% 범위로 제한
- 사이드바 내용(프로젝트가 많거나 "⚙ 설정"을 펼쳤을 때)이 화면 높이를 넘으면 **사이드바 자체가 내부 스크롤**된다(`overflow-y-auto`) — 넘친 내용이 하단 단축키 바 위로 겹쳐 보이는 걸 방지
- **"⚙ 설정"을 펼치면 펼쳐진 내용이 보이도록 자동으로 스크롤한다**: 브라우저 기본 동작(`<details>` 펼침)만으로는 스크롤이 안 따라가는 경우가 있어(특히 처음 펼칠 때 — 새로 생긴 높이를 반영하기 전에 시도해서 그런 것으로 추정) `onToggle`에서 직접 스크롤 위치를 계산해서 옮겨준다(`Sidebar.tsx`)
- 하단 단축키 바는 **왼쪽 사이드바 폭만큼 `margin-left`를 둬서** 시작하고, 사이드바 폭을 드래그하는 동안 실시간으로 같이 따라온다(`src/lib/sidebarWidth.ts`의 커스텀 이벤트로 두 컴포넌트가 폭을 공유)

### 5.8 캘린더 뷰 (`/calendar`, `src/app/calendar/page.tsx`)
- 사이드바에 "전체/오늘/예정/완료" 아래, "주간보고" 위에 별도 진입점으로 추가됨 (`?view=` 파라미터 체계와는 무관한 독립 페이지)
- **"← 이전 달"/"오늘"/"다음 달 →" 버튼은 월 표시("YYYY년 M월") 바로 옆에 붙어 있다** (예전엔 헤딩과 반대쪽 끝에 떨어져 있었음)
- **월간 그리드 + 사이드 패널** 하이브리드 구조: 왼쪽은 한 달 전체를 보여주는 그리드, 오른쪽은 그리드에서 선택한 날짜의 태스크를 지금 목록 뷰와 동일한 `TaskList`/`TaskItem`으로 그대로 보여준다 — 완료/삭제/다중선택/서브태스크 추가·드래그 등 목록 뷰의 모든 조작이 패널에서 동일하게 동작함
- 그리드에 표시되는 태스크는 **완료되지 않은(마감일이 있는) 최상위 태스크만** (서브태스크·완료된 태스크는 다른 뷰와 동일하게 제외)
- 칸 하나에는 칩을 **최대 3개**까지 표시하고, 그 이상은 "+N개"로 접는다 — 클릭하면(=그 날짜를 선택하면) 패널에 전체 목록이 뜨므로 접힌 항목도 거기서 전부 확인 가능
- 칩의 색 점은 **우선순위 색**을 사용 (체크박스 테두리 색과 동일한 규칙 — `priorityColors`)
- 칸 안 칩 순서·사이드 패널·"미등록 일정" 모두 5.2절과 동일한 정렬 규칙(우선순위 → 생성일)을 따른다
- 그리드는 해당 월의 1일이 속한 주부터 마지막 날이 속한 주까지 꽉 채워서 보여주며, 앞뒤 달에 걸친 칸은 흐리게 표시하되 그 날짜의 태스크도 동일하게 보여줌(달력에 빈 줄이 생기지 않도록)
- **패널은 항상 "선택된 날짜"를 기준으로 뜬다**: 처음 진입하면 오늘, 그리드의 다른 날짜를 클릭하면 그 날짜로 전환. "이전 달"/"다음 달"로 달을 넘기면 선택 날짜가 새 달의 1일로 자동으로 맞춰진다(안 그러면 그리드에 보이지도 않는 지난달 날짜가 패널에 그대로 남아 헷갈림). "오늘" 버튼은 이번 달 + 오늘 날짜로 이동
- **칸을 더블클릭하면 그 날짜를 마감일로 미리 채운 채 새 할일 팝업(`QuickAddModal`)이 열린다** (`CalendarDayCell` 컴포넌트가 `window` 커스텀 이벤트 `quickadd:open`을 날리고, 모든 페이지에 이미 마운트된 `QuickAddModal`이 그 날짜를 받아 마감일 입력창에 미리 채워서 연다). 더블클릭의 개별 클릭들이 먼저 그 날짜를 선택하는 것도 그대로 같이 일어남(부가효과, 의도된 동작)
- **프로젝트 사이드바 필터가 캘린더에도 연결됨**: 캘린더 페이지에서 사이드바의 프로젝트를 누르면 `/calendar?project=...`로 이동해 그리드·사이드 패널·"미등록 일정" 세 곳 모두 그 프로젝트 태스크만 보여준다(목록 뷰와 동일하게 완전히 좁혀서 보여주는 방식). 프로젝트를 눌러도 현재 보고 있던 달/선택 날짜(`month`/`date` 쿼리)는 그대로 유지된다 — "전체 프로젝트"를 누르면 필터만 해제되고 역시 같은 달/날짜에 머무른다. 목록 뷰(`/`)와 달리 캘린더 페이지에서는 사이드바 프로젝트 링크가 `/calendar`로 향하도록 분기해뒀다(`Sidebar.tsx`의 `isCalendarPage`+`calendarMonth`/`calendarDate` props)
- **캘린더/패널 아래에 "미등록 일정" 섹션**: 마감일이 아예 없는(그래서 그리드 어디에도 걸릴 수 없는) 미완료 최상위 태스크를 접이식(`CollapsibleSection.tsx`)으로 모아 보여준다. 기본은 접힘, 클릭하면 펼쳐지고 안의 목록은 패널과 동일하게 `TaskList`를 재사용해 완료·삭제 등 전부 그대로 동작한다. 이 목록은 어느 달을 보고 있는지와 무관하게(달 이동해도) 항상 전체를 보여줌
  - **펼치면 그 위치가 보이도록 자동으로 아래로 스크롤하고, 접으면 펼치기 전 스크롤 위치로 되돌아간다**("⚙ 설정"과 동일한 이유·관례, 5.7.1절 참고). 펼치기 전 스크롤 위치를 기억해뒀다가 접을 때 정확히 그 자리로 복원한다(항상 맨 위로 가는 게 아니라, 사용자가 보고 있던 곳으로)
  - 이 섹션을 펼쳤을 때 항목이 많으면(꽤 많이 쌓일 수 있음) 그리드/패널 영역과 시각적으로 겹쳐 보이던 버그가 있었다: 그리드/패널을 감싸는 컨테이너가 `flex-1`(+`min-h-0`)로 "남은 공간을 채우도록" 되어 있었는데, 아래 "미등록 일정"이 펼쳐져 커지면 `main`의 세로 공간을 두고 서로 경쟁하다가 `min-h-0`가 걸린 그리드/패널 쪽이 0까지 짜부라지고, 그 안의 실제 내용(그리드 셀, 목록)은 원래 크기 그대로라 화면 밖으로 삐져나와 겹쳐 보였다(펼칠수록 심해진 이유). 애초에 그리드/패널이 "남은 공간 채우기"로 늘어날 필요가 없었으므로(각 칸이 `min-h-[92px]`라 6주짜리 달이어도 자연스러운 고정 높이가 나옴), `CalendarSplit.tsx`의 바깥 컨테이너에서 `flex-1`/`min-h-*`를 아예 없애고 **내용물 그대로의 자연스러운 높이**만 갖게 했다 — 이제 그리드/패널은 항상 자기 크기만큼만 차지하고, "미등록 일정"은 그 바로 아래 제자리에 오며, 전체가 넘치면 `main`의 스크롤(`overflow-y-auto`)로만 처리된다
  - **10개 넘어가면 페이지네이션**: 위 겹침 버그와는 별개로, 항목 수 자체가 많아지는 것에 대비해 `PaginatedTaskList.tsx`로 감싸서 **한 페이지에 10개씩** 보여주고 "이전/다음" 버튼 + "N / 총페이지" 표시를 추가했다(추가 서버 조회 없이, 이미 불러온 전체 목록을 클라이언트에서 잘라서 보여줌). 완료·삭제로 목록이 줄어 현재 페이지가 범위를 벗어나면 자동으로 마지막 페이지로 당겨온다

## 6. Phase 2 (계획 · 미구현) 📋

- 완료 통계 / 생산성 리포트
- 자연어 입력 파싱 (예: "내일 3시 보고서" → 자동 날짜 인식)
- 다크모드 토글, 키보드 단축키
- 태그별 필터 UI

## 7. Phase 3: 주간보고 ✅ 구현 완료

### 7.1 후보 데이터 추출 로직 (`src/app/report/page.tsx`)
선택한 주간 범위(기본 이번 주 월~일, `?week=YYYY-MM-DD` 쿼리로 다른 주 조회 가능, 페이지 상단 "지난 주/다음 주" 링크로 이동) 기준으로 두 후보 목록을 자동 추출한다.

- **완료 업무 후보**: `completedAt`이 선택한 주간 범위 내인 **최상위** 태스크
- **완료된 서브태스크 후보**: `completedAt`이 선택한 주간 범위 내인 서브태스크(`parentId != null`)도 완료 후보 목록에 개별로 포함됨. 제목은 `상위 태스크 제목 › 서브태스크 제목` 형식으로 표시해서 어느 상위 소속인지 알 수 있게 하고, 기본적으로 체크되어 보고서에 바로 포함됨 (완료 후보와 같은 정렬·같은 기본 체크 규칙 적용, 상위 태스크 이름으로만 뭉뚱그려 보이던 문제 수정)
- **진행 중 / 미완료 업무 후보**: `completed=false` 이면서 아래 중 하나에 해당하는 최상위 태스크
  - `dueDate`가 선택한 주간 범위 내 (이번 주가 기한이었는데 못 끝낸 것)
  - `updatedAt`이 선택한 주간 범위 내 (이번 주에 생성되었거나 내용에 진전이 있었던 것 = "진행된 내용")
  - **서브태스크 중 하나라도 `completedAt`이 선택한 주간 범위 내인 경우** (상위 태스크 자체는 아직 안 끝났지만 하위 항목 하나를 이번 주에 끝낸 것도 "진행중"으로 잡음 — 이 서브태스크는 위 "완료된 서브태스크 후보"에도 별도로 뜨므로 진행중 후보의 상위 태스크와 완료 후보의 서브태스크 항목에 동시에 나타날 수 있음, 의도된 동작)

### 7.2 선택(Select) UI (`src/components/WeeklyReportBuilder.tsx`)
- 후보는 자동으로 뽑아 보여주되, **보고서에 실제로 포함할지는 사용자가 체크박스로 직접 선택**한다.
- 기본 체크 상태: **완료 후보는 전체 체크**, **진행/미완료 후보는 전체 체크 해제**로 시작한다 (자동으로 이것저것 다 올라가는 오탐 방지 — 사용자가 올릴 것만 직접 고른다).
- 체크 상태를 바꾸면 우측 마크다운 미리보기가 즉시 갱신된다 (클라이언트 상태, 저장되지 않음 — 새로고침하면 기본값으로 리셋).

### 7.3 출력 포맷 (`src/lib/weeklyReportTemplate.ts` — 렌더링 로직을 별도 함수로 분리, 회사 양식이 정해지면 이 함수만 교체)

```markdown
## 주간업무보고 (YYYY-MM-DD ~ YYYY-MM-DD)

### ✅ 금주 완료 업무
- [프로젝트명] 업무 제목 (완료일: MM/DD)

### 🔄 진행 중 / 미완료 업무
- [프로젝트명] 업무 제목 — 마감: MM/DD (마감일 없으면 "진행 중")
```

- 화면에 렌더링 + "복사" 버튼으로 클립보드 복사 (`navigator.clipboard.writeText`, 실패 시 조용히 무시하고 미리보기에서 수동 복사 가능)
- 회사 정식 양식은 나중에 별도로 전달 예정 → `weeklyReportTemplate.ts`의 `renderWeeklyReport()` 함수만 교체하면 되도록 로직과 포맷을 분리해서 구현함

## 8. 변경 관리 원칙 (중요)

- 이 문서는 프로젝트 요구사항의 **단일 기준(SSOT)** 이다.
- 새 기능·수정을 요청할 때는 가능하면 **몇 절을 어떻게 바꾸는지** 구체적으로 지정한다 (예: "7.2절을 이렇게 바꿔줘").
- 별다른 언급이 없는 한, 이미 구현되어 5절에 명시된 기능과 6~7절의 계획 범위는 **그대로 유지한 채 추가/구현만** 진행한다. 관련 없는 기능을 임의로 바꾸지 않는다.
- 기능을 변경하거나 없애야 할 때는 이 문서도 함께 갱신해서 항상 코드와 문서 상태가 일치하도록 한다.

## 9. 알려진 제한사항 / 엣지 케이스 (버그는 아니지만 인지하고 있어야 하는 동작)

- **자동화 테스트 없음**: `package.json`에 `test` 스크립트 자체가 없다. 지금까지 모든 검증은 `npx tsc --noEmit` + `npm run lint` + 브라우저 수동 조작(+ 필요시 `better-sqlite3`로 DB 직접 조회)으로만 해왔음
- **로컬 SQLite라 `dev.db`/`.env`는 git에 없음**(`.gitignore`에서 제외): 새 환경(새 PC, 새 클론)에서는 `.env`에 `DATABASE_URL="file:./dev.db"` 직접 생성 + `npx prisma migrate dev` 또는 `npx prisma db push`로 스키마부터 새로 만들어야 실행됨. 기존 데이터는 옮겨지지 않음
- **`d`/`r` 다중 선택 단축키는 `Ctrl/Alt/Meta`가 눌려있으면 무시됨**. `Shift+d`는 이제 "완료 취소"로 배정됨(5.1.2절). `Shift+r`은 마땅한 반대 동작이 없어 계속 예약만 해두고 비워둔 상태 (`src/components/TaskList.tsx`)
- **서브태스크 위에 드롭해도 그 서브태스크 자신의 하위로는 못 들어감**: 드래그 앤 드롭 핸들러가 각 서브태스크 행이 아니라 최상위 태스크 `<li>` 전체에 걸려 있어서(이벤트 버블링), 어느 서브태스크 위에 놓아도 결국 "그 서브태스크가 속한 최상위 태스크"의 하위로 편입된다 — 5.5절의 "서브태스크는 1단계까지만" 제약이 드래그 앤 드롭에서도 자연히 유지되는 이유

## 10. 성능 · 실행 환경

기능은 그대로 두고 반응/저장/로드 속도만 최적화한 결과. 아래 항목은 화면에 보이는 동작을 바꾸지 않는다.
회차별 변경 이력과 측정값, 검토했으나 적용하지 않은 것들은 [`PERF.md`](PERF.md)에 정리되어 있다.

### 10.1 앱을 "쓸" 때는 개발 서버로 띄우지 않는다 (가장 큰 차이)

- `dev.cmd`(=`next dev`)는 **개발용**이다. 요청마다 라우트를 컴파일하고 최적화를 끈 React를 쓰기 때문에 같은 화면이라도 훨씬 느리다.
- 실사용은 **`run.cmd`**(=`npm run build && npm start`)로 띄운다. 이 PC 기준 서버 렌더링 `46ms → 14ms`(3.3배), 초기 HTML `35.4KB → 27.6KB`.
- 코드를 고친 뒤에는 `run.cmd`를 다시 실행해야 반영된다 (개발 서버와 달리 자동 반영 안 됨). 기능을 개발하는 중에는 그대로 `dev.cmd`를 쓰면 된다.

### 10.2 SQLite WAL 모드 (저장 속도)

- `src/lib/prisma.ts`가 부팅 시 `PRAGMA journal_mode = WAL`을 한 번 설정한다. 기본값(`delete`)은 커밋마다 저널 파일을 만들고 지우며 fsync를 두 번 한다.
- 이 PC 기준 단일 쓰기 지연 `1.42ms → 0.03ms` (약 48배). `journal_mode`는 DB 파일 헤더에 저장되는 영구 설정이라 한 번만 켜면 이후 모든 연결이 이어받는다.
- 부수 효과로 `dev.db-wal`, `dev.db-shm` 파일이 생긴다 (gitignore 처리됨). **DB를 다른 곳으로 복사할 때는 이 두 파일도 같이 옮기거나, 앱을 끈 상태에서 복사할 것.**

### 10.3 인덱스 (로드 속도)

- 마이그레이션 `20260908000049_add_indexes`. 그 전에는 Task/TaskTag에 인덱스가 하나도 없어서 모든 조회가 전체 스캔이었다.
- `Task(parentId, completed, dueDate)` — 4개 뷰 전부가 이 순서로 걸러서 하나로 커버됨 / `Task(parentId)` — 서브태스크 로딩 / `Task(projectId)` — 프로젝트 필터·자동정리 / `Task(completed, completedAt)` — 주간보고 / `TaskTag(tagId)` — 태그 자동정리·태그별 칸반 (복합 PK 선두가 `taskId`라 `tagId` 단독 조회는 인덱스를 못 탔음)
- 현재 데이터량(태스크 수십 건)에서는 체감 차이가 없다. 태스크가 수천 건으로 늘어날 때를 위한 것.

### 10.4 서버 왕복 줄이기

- **`revalidatePath("/")` → `refresh()`** (Next 16, `next/cache`): 전자는 이전에 방문한 다른 페이지까지 다시 불러오게 만든다. `refresh()`는 지금 보고 있는 라우트만 갱신한다. 덤으로 주간보고 화면에서 `n` 팝업으로 태스크를 추가해도 그 화면이 바로 갱신된다.
- **페이지 쿼리 병렬화**: 목록/주간보고 페이지가 프로젝트·태그·우선순위색·태스크를 순차로 `await` 하던 것을 `Promise.all` 하나로 묶었다.
- **한 액션 = 한 트랜잭션**: 완료 처리와 반복 일정 다음 회차 생성을 `$transaction`으로 묶어 커밋(=fsync)을 한 번만 한다.
- **자동정리 쿼리 축소**: `cleanupUnusedProjectAndTags`가 후보마다 `count`를 날리던 것(1+N 왕복)을 `groupBy` 두 번으로 바꿨다. 삭제도 한 번에 묶는다.

### 10.5 클라이언트

- **쓰지도 않는 웹폰트 제거**: `layout.tsx`가 Geist Sans/Mono를 불러오고 있었지만 `globals.css`의 `body { font-family: Arial, ... }`가 덮어써서 **한 번도 렌더에 쓰이지 않았다**. 걷어냄 — 화면은 그대로다.
- **`TaskItem`을 `memo`로 감쌈**: 태스크 하나를 선택하면 목록 전체가 다시 렌더되던 것을, 실제로 달라진 행만 렌더하도록 바꿈 (`TaskList`가 콜백을 `useCallback`으로 안정화).
- **칸반 그룹핑 1회 순회**: 컬럼마다 `tasks.filter`를 돌려 태스크×컬럼 만큼 비교하던 것을 한 번 순회하며 나눠 담는 방식으로 바꾸고 `useMemo`로 감쌌다.

### 10.6 측정값 요약 (이 PC 기준, 태스크 26건)

| 항목 | 전 | 후 |
|---|---|---|
| 페이지 서버 렌더 | 46.4ms (dev) | 14.0ms (prod) |
| 단일 DB 쓰기 | 1.42ms | 0.03ms |
| 태스크 추가 왕복(저장→화면반영) | — | 약 10ms |
| 다중 선택 N개 완료/삭제 | 서버 왕복 N회 | 1회 |
| 완료/삭제 클릭 반응 | 서버 응답 후 | 즉시(낙관적) |
| 초기 HTML | 35.4KB | 27.6KB |
| 웹폰트 요청 | Geist 2종 | 없음 |

### 10.7 일일 자동 백업 (`src/lib/prisma.ts`)

- 상시 켜져있는 서버가 아니라 그때그때 실행하는 앱이라 OS cron 없이는 "매일 자동으로" 뭔가를 할 방법이 없다. 그래서 **부팅 시점마다 "오늘 이미 백업했나"를 확인**해서, 안 했으면 그때 `dev.db`(+`-wal`/`-shm`)를 통째로 `backups/dev-YYYY-MM-DD.db`로 복제한다 (WAL 모드 부팅 체크와 동일한 `globalThis` 플래그 패턴으로 프로세스당 1회만 확인)
- **이관이 아니라 순수 복제**: 사용 중인 `dev.db`에서는 아무것도 지우거나 옮기지 않는다. 그래서 주간보고 등 앱의 모든 쿼리는 날짜와 무관하게 항상 `dev.db` 하나만 보면 된다 — "이 기간은 백업 파일에서 찾아야 하나"를 판단하는 로직이 아예 필요 없다
- **최신 7개만 보관**(파일 개수 기준, 날짜 범위 기준 아님) — 며칠 안 켜서 건너뛴 날이 있어도 "최근 7일" 범위 계산 때문에 전부 오래된 걸로 지워지는 일이 없다
- 앱은 평소에 `backups/` 폴더를 전혀 읽지 않는다. 순전히 재해 복구용(디스크 고장 등)이며, 복구는 수동으로 백업 파일을 `dev.db` 자리에 덮어쓰는 방식
- `backups/`는 `.gitignore`에 추가됨(커밋 안 됨)
- best-effort: 폴더 생성·복사·정리 전부 `try/catch`로 감싸 실패해도(권한 문제 등) 앱 부팅에 영향 없음
- 실제 dev.db(태스크 33건 기준)로 검증: 진짜 부팅 경로로 오늘 백업이 정상 생성됐고(내용이 라이브 DB와 정확히 일치), 스크립트로 8일치를 더 시뮬레이션해 **최신 7개 유지 + 오래된 것 자동 정리 + 같은 날 재실행 시 스킵(idempotent)** 전부 확인. 시뮬레이션용 가짜 날짜 스냅샷은 검증 후 삭제하고 진짜 오늘자 백업만 남김
