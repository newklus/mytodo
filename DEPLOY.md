# 배포 파일 실행법 가이드

MyTodo를 "웹사이트"가 아니라 **PC 앱처럼** 쓰기 위한 절차. 기능 스펙은 `requirements.md`, 이 설정의 배경/설계 근거는 `requirements.md` 10.10절 참고.

## 1. 빌드

```bash
npm run build
```

`.next/` 아래에 프로덕션 산출물이 생긴다. 코드를 고칠 때마다 다시 실행해야 반영된다(개발 서버와 달리 자동 반영 안 됨).

## 2. 서버 실행

```bash
npm start
```

또는 빌드까지 한 번에:

```bash
run.cmd
```

뜨는 주소: **http://localhost:3333** (3000번은 다른 용도로 예약돼 있어 3333번을 씀)

## 3. 앱처럼 설치하기 (PWA)

1. 서버가 떠 있는 상태에서 브라우저로 위 주소에 접속
2. 주소창 오른쪽의 **설치 아이콘**(⊕ 모양) 클릭 → "설치"
   - Edge 기준: 주소창 아이콘이 안 보이면 `⋯` 메뉴 → "앱" → "이 사이트를 앱으로 설치"
3. 설치되면 시작 메뉴/작업표시줄에 독립된 앱 아이콘이 생긴다. 이후로는 이 아이콘으로 열면 주소창 없는 창으로 뜬다
4. 이미 앱이 떠 있는 상태에서 아이콘을 또 누르면 새 창이 아니라 **기존 창이 그대로 포커스**된다(중복 창 안 뜸)

**주의**: 이 아이콘은 `http://localhost:3333`을 열 뿐, 서버를 대신 켜주지 않는다. 서버가 안 떠 있으면 접속 실패 화면이 뜬다 → 4절로 이어짐.

## 4. 로그인 시 서버 자동으로 켜두기 (선택, 권장)

매번 `run.cmd`/`npm start`를 손으로 실행하기 싫으면, PC 로그인할 때 서버가 백그라운드로 자동 기동되게 등록할 수 있다.

**등록**:
```powershell
powershell -ExecutionPolicy Bypass -File autostart-install.ps1
```
- 작업 스케줄러에 `MyTodoServer` 태스크가 생성됨 (관리자 권한 불필요)
- 다음 로그인부터 자동으로 서버가 뜸(콘솔 창 안 보임)
- 지금 바로 테스트하려면: `Start-ScheduledTask -TaskName 'MyTodoServer'`

**제거**:
```powershell
powershell -ExecutionPolicy Bypass -File autostart-uninstall.ps1
```

⚠️ **PWA 설치와 이 자동기동 등록은 서로 완전히 별개다.** 하나를 지운다고 다른 하나가 같이 없어지지 않으므로, 둘 다 그만 쓰려면 각각 따로 해제해야 한다 — PWA는 브라우저에서 "앱 제거", 자동기동은 위 제거 스크립트.

## 5. 평소 사용 흐름 정리

| 상황 | 할 일 |
|---|---|
| 처음 설정 | 1(빌드) → 4(자동기동 등록) → 3(PWA 설치) |
| 코드를 고친 뒤 | 1(재빌드)만 다시. 서버는 자동기동이 이미 떠 있으면 다음 로그인 때 새 빌드로 자동 반영 |
| 그냥 쓸 때 | 시작 메뉴/작업표시줄의 설치된 앱 아이콘만 클릭 |
| 개발 중일 때 | 이 가이드 말고 `dev.cmd` 사용 (HMR 자동 반영, 포트는 동일하게 3333) |

## 6. 문제 해결

- **PWA 아이콘을 눌렀는데 접속 실패**: 서버가 안 떠 있는 것. 자동기동을 등록 안 했으면 `run.cmd`로 직접 켤 것
- **포트 3333이 이미 쓰이는 중**: 이 프로젝트의 서버가 이미 떠 있을 가능성이 큼(자동기동으로 켜졌거나 수동으로 이미 실행 중) — 새로 또 켤 필요 없이 그냥 접속하면 됨. 다른 프로그램이 3333을 쓰고 있다면 `package.json`의 `-p 3333`을 다른 번호로 바꾸고 `.claude/launch.json`도 맞춰줄 것
- **로그인해도 자동으로 안 켜짐**: `Get-ScheduledTask -TaskName MyTodoServer`로 등록 여부 확인, `Start-ScheduledTask -TaskName MyTodoServer`로 수동 트리거해서 에러 확인
- **자동기동은 도는데 화면이 옛날 버전**: 로그인 시점엔 이미 있는 빌드를 그대로 쓰기 때문(재빌드 안 함, 10.10절 참고) — 코드를 고쳤으면 `npm run build`를 한 번 더 실행해야 함

## 7. 새 PC로 옮길 때

이 문서는 "실행 방법"만 다룬다. DB(`dev.db`)를 새 환경으로 옮기는 절차는 아직 정식으로 마련돼 있지 않음 — `requirements.md` Phase 4 항목으로 등록돼 있고, 그전까지는 `HANDOVER.md` 3.1절의 DB 관련 주의사항(WAL 파일 함께 옮기기 등)을 참고할 것.
