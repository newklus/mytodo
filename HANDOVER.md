# MyTodo 인수인계서

> 작성: 2026-09-08 (4회차 세션 종료 시점). 새 창(새 Claude Code 세션)에서 이어서 작업할 때 먼저 읽을 문서.
> **가장 먼저 [`requirements.md`](requirements.md)를 전체 읽을 것** — 이 문서는 "지금 상태로 오게 된 맥락과 다음에 뭘 하면 좋을지"만 요약한 보조 문서이고, 기능 스펙의 단일 기준(SSOT)은 항상 `requirements.md`다.

## 1. 이 프로젝트가 뭔지

- Claude Code 사용법 학습 + 효율적인 프롬프트 작성법 연습용 실습 프로젝트 (배경: [`BG.md`](BG.md))
- 실습 소재로 실제 회사 업무에 쓸 수 있는 **개인용 TODO 앱**을 Next.js로 만들고 있음
- 사용자는 한국어로 요구사항을 배치 단위로 던지고, 그때마다 구현 후 `requirements.md`를 갱신하는 방식으로 진행해왔음

## 2. 지금 당장 실행하는 법

| 목적 | 실행 |
|---|---|
| 앱을 실제로 쓸 때 | `run.cmd` (프로덕션 빌드, 코드 고치면 재실행 필요) |
| 코드를 개발/수정할 때 | `dev.cmd` (HMR 자동 반영, 느림) |

- 둘 다 http://localhost:3000
- Node가 PowerShell PATH에 안 잡혀 있으면: `$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")` (`dev.cmd`/`run.cmd`는 내부에서 처리하므로 무관)
- **DB**: SQLite, `.env`의 `DATABASE_URL`이 가리키는 `dev.db` (`dev.db`/`.env` 둘 다 gitignore). 처음 세팅하는 환경이면 `.env`에 `DATABASE_URL="file:./dev.db"` 만들고 `npx prisma migrate dev`
- 이미 이 PC에서 개발 중이라면 `dev.db`/`.env`가 이미 있고 **사용자의 실제 태스크 데이터**가 들어있음 — 절대 삭제/초기화하지 말 것
- DB는 WAL 모드다. `dev.db-wal`/`dev.db-shm`이 같이 생기고, 최신 데이터가 이 두 파일에 있을 수 있으니 수동으로 옮길 땐 세 파일을 같이
- **마이그레이션은 전부 적용된 상태다** (`npx prisma migrate status` → up to date). 3회차에 밀려있던 `20260908023836_default_priority_p3`도 이번 세션 초반에 적용 완료함
- 부팅할 때마다 `backups/dev-YYYY-MM-DD.db`가 자동 생성된다 (최신 7개 유지, `requirements.md` 10.7절). 앱은 이걸 안 읽으니 무시해도 되고, 재해 복구용으로만 존재
- **세션 종료 시점에 이 세션이 띄워놨던 dev 서버는 꺼놨음** (포트 3000 비어있음). 새 세션에서 `dev.cmd`/`run.cmd`를 그냥 실행하면 됨
- 작업 완료 기준: **항상** `npx tsc --noEmit` + `npm run lint` 통과. UI 변경은 브라우저로 직접 클릭 확인

## 3. 지금까지 진행 상황

- **커밋 이력은 3회차(`0f2c99f`)에서 멈춰 있음 — 이번 4회차에서 한 작업은 전부 미커밋 상태다.** `git status`로 확인하면 수정 파일 9개 + 신규 파일 10개(아래 4.1절 목록)가 그대로 워킹 트리에 있음. 커밋/푸시는 사용자가 명시적으로 요청 안 했으므로 그대로 뒀음 — 다음 세션에서 요청받으면 커밋할 것
- 1~3회차 요약은 `requirements.md` 5~10절, `PERF.md` 참고 (전작: 기능 스캐폴딩 → 성능 최적화 → 버그수정/캘린더/undo/백업)
- **4회차(이번 세션)에서 한 일** — 전부 사용자가 UI를 실제로 써보면서 "이거 이상해/겹쳐/불편해" 식으로 하나씩 던진 걸 그때그때 고친 것이라 목록이 김:
  1. **밀려있던 마이그레이션 적용** (우선순위 기본값 4→3)
  2. **완료 뷰 15일 제한**: `completedAt >= 15일 전`만 화면에 표시 (DB엔 그대로 남음)
  3. **태스크 선택/편집 인터랙션 전면 개편**:
     - **일반 선택 vs 다중 선택 구분**: 조합키(기본 `Ctrl`, 재배정 가능) 없이 클릭하면 이전 선택 다 지우고 이 태스크만 선택(탐색기류 단일 선택). 조합키를 누른 채 클릭하면 기존 선택 유지하며 토글
     - 이미 그 태스크 하나만 선택된 상태에서 본문을 다시 클릭해야 편집 모드로 들어감 (아이콘 클릭은 항상 선택 동작, 편집으로 안 이어짐)
     - 호버 시 우선순위색 테두리가 아이콘까지 포함한 영역에 나타남 (아이콘+본문을 하나의 wrapper로 묶음)
  4. **정렬 규칙 통일**: 완료 뷰 제외 모든 리스트/캘린더는 우선순위→생성일순. 완료 뷰만 예외로 최신 완료일 우선(우선순위 무관)
  5. **캘린더**: 이전달/오늘/다음달 버튼을 월 표시 바로 옆으로 이동. 그리드/패널 크기 조절 가능(드래그). "미등록 일정"을 펼치면 그리드와 겹쳐 보이던 버그 수정(원인: `flex-1`+`min-h-0`로 "남은 공간 채우기"를 하려다 짜부라짐 — 애초에 불필요해서 제거하고 내용물 그대로의 자연스러운 높이만 갖게 함). "미등록 일정"도 펼치면 스크롤 다운, 접으면 이전 위치로 스크롤 업
  6. **레이아웃 크기 조절**: 사이드바 너비, 캘린더 그리드/패널 비율을 드래그로 조절 가능, `localStorage`에 저장돼 다음 접속 때 로드
  7. **화면 하단 단축키 바 신설**(`ShortcutBar.tsx`): 모든 페이지에 항상 표시. 기본 🔒 잠금 상태(새로고침마다 리셋), 잠금 해제 후 칩 클릭 → 새 키 입력으로 재배정. `Enter` 단독은 배정 불가(취소 처리). 다중 선택 조합키도 여기서 재배정 가능. 잠금 아이콘은 이모지 대신 회색조 SVG
  8. **태스크 메모 기본 표시**: 목록에서 메모 내용이 기본으로 펼쳐져 보이고 클릭으로 접기/펼치기. 기본값(펼침)은 설정에서 바꿀 수 있음
  9. **시작 페이지 설정**: `/`를 파라미터 없이 열었을 때(=앱을 새로 켰을 때) 오늘/전체/캘린더 중 원하는 화면으로 가게 설정 가능(쿠키 저장). 설정을 고르면 즉시 그 화면으로 이동함(처음엔 쿠키만 바꾸고 안 옮겨서, 다른 페이지에 머문 채로는 설정이 반영 안 되는 것처럼 보이는 버그가 있었음 — 실제로 사용자가 겪고 리포트해서 고침)
  10. 사이드바 겹침 버그 수정: 사이드바 내용이 넘칠 때 `overflow-y-auto`로 내부 스크롤(하단 단축키 바와 안 겹치게), "⚙ 설정" 펼칠 때 처음엔 스크롤이 안 따라가던 문제 수정(직접 스크롤 위치 계산)
  11. 선택 개수 표시 위치 변경: 목록 위 안내 바(단축키 힌트 포함) 제거하고 "N개 선택됨"만 페이지 헤딩 옆으로 이동

## 4. 코드를 새로 읽지 않으면 놓치기 쉬운 것들

### 4.1 이번 세션에서 새로 생기거나 크게 바뀐 파일

**신규 파일**
- `src/lib/shortcuts.ts` — 키보드 단축키(`n`/`d`/`Shift+d`/`r`/`Esc`/`u`) 바인딩 + 다중 선택 조합키(`ModifierKey`) 관리. `localStorage` 저장, `useShortcuts()`/`useMultiSelectModifier()` 훅
- `src/lib/sidebarWidth.ts` — 사이드바 폭을 `Sidebar.tsx`(드래그로 변경)와 `ShortcutBar.tsx`(마진 계산용)가 공유. `window` 커스텀 이벤트로 실시간 동기화
- `src/lib/memoSettings.ts` — 메모 기본 펼침 여부 (`localStorage`)
- `src/lib/selectionCount.ts` — `TaskList`의 선택 개수를 페이지 헤딩 옆 `SelectionCountBadge`로 전달 (`window` 커스텀 이벤트, `undoBus`/`quickadd:open`과 동일 관례)
- `src/lib/startPage.ts` — 시작 페이지 설정. **다른 lib 파일들과 달리 `"use client"`가 없는 순수 유틸**이다 — 서버(`page.tsx`, `cookies()`)와 클라이언트(`Sidebar.tsx`, `document.cookie`) 양쪽에서 같은 타입/쿠키 이름을 써야 해서 의도적으로 그렇게 함. **localStorage가 아니라 쿠키를 쓰는 유일한 설정**이다(서버 첫 렌더에서 바로 반영해야 깜빡임이 없어서)
- `src/components/ShortcutBar.tsx` — 화면 하단 단축키 바. `layout.tsx`에 전역 마운트
- `src/components/CollapsibleSection.tsx` — "⚙ 설정"과 동일한 패턴(펼치면 스크롤 다운, 접으면 복원)을 캘린더 "미등록 일정"에도 쓰려고 뽑아낸 컴포넌트. **`Sidebar.tsx`의 "⚙ 설정"은 이 컴포넌트를 안 쓰고 자기만의 로직을 그대로 갖고 있다** — 스크롤 대상이 `aside`냐 `main`이냐가 달라서 통합 안 하고 중복을 허용함(4.2절 참고)
- `src/components/CalendarSplit.tsx` — 캘린더 그리드/패널 크기 조절 가능한 분할. **`flex-1`/`min-h-*` 없이 내용물 그대로의 자연스러운 높이만 가짐**(4.2절 참고 — 예전에 `flex-1 min-h-0`였다가, 그다음 `min-h-[50vh]`로 고쳤다가, 최종적으로 아예 제거했다. 다시 `flex-1`을 넣고 싶어지면 이 히스토리를 먼저 볼 것)
- `src/components/SelectionCountBadge.tsx` — 페이지 헤딩 옆 "N개 선택됨" 표시

**크게 바뀐 파일**
- `src/components/TaskItem.tsx` — 아이콘+본문을 감싸는 wrapper 하나로 호버 테두리 처리, 클릭 핸들러가 `isModifierPressed()`로 일반/다중 선택 분기. `isSoleSelection` prop(부모가 계산해서 내려줌)으로 "이미 이 하나만 선택된 상태"를 판단해 편집 진입 여부 결정
- `src/components/TaskList.tsx` — `toggleSelect`(다중, 토글) / `selectOnly`(일반, 교체) 두 콜백을 `TaskItem`에 내려줌. 선택 개수는 `announceSelectionCount`로 밖으로 쏘고 자기 안에는 더 이상 안내 바를 안 그림
- `src/components/Sidebar.tsx` — 리사이즈 가능(드래그), "⚙ 설정" 안에 메모 기본값/시작 페이지 설정 추가, 사이드바 자체가 `overflow-y-auto`
- `src/app/page.tsx` — `view` 파라미터 없을 때 쿠키(`mytodo_start_page`) 확인해서 "calendar"면 `redirect()`, "all"이면 `view="all"`로 렌더

### 4.2 사이드바 설정 스크롤과 캘린더 미등록 일정 스크롤이 로직 중복인 이유

`Sidebar.tsx`의 "⚙ 설정"과 `CollapsibleSection.tsx`(캘린더 "미등록 일정")는 **똑같은 목적**(펼치면 그 위치로 스크롤, 접으면 이전 위치로 복원)을 갖고 있지만 **코드가 하나로 안 합쳐져 있다**. 이유: 스크롤 대상 컨테이너가 다르다 — 사이드바는 `<aside>` 자기 자신이 스크롤되고(요소 자신에 `ref` 하나면 됨), 캘린더 쪽은 `<details>`가 `<main>` 안에 있어서 `details.closest("main")`으로 스크롤 컨테이너를 찾아야 한다. 게다가 사이드바 쪽은 "펼치면 아래로, 접으면 이전 위치로 복원"이 아니라 "펼치면 무조건 그 위치가 보이게"만 하면 되는 반면(사이드바는 대체로 짧아서), 캘린더 쪽은 접었을 때 "펼치기 전 스크롤 위치를 기억했다가 복원"하는 로직이 추가로 있다. 새로 비슷한 접이식 UI를 또 만들 일이 있으면, 두 구현을 비교해보고 그때 가서 진짜 공통분모로 뽑을지 판단할 것 — 지금은 섣불리 합치지 않았다.

### 4.3 이번 세션에서 겪은 삽질 — 다음에 비슷한 걸 겪으면 참고

- **브라우저 탭을 재사용하면서 파일을 여러 번 고치면(Fast Refresh 반복), 예전 콘솔 에러가 안 지워지고 계속 쌓여서 `read_console_messages`에 나온다.** 실제로 이미 고쳐진 문제가 마치 지금도 나는 것처럼 보여서 두 번이나 헛다리를 짚었다(한 번은 React 훅 배열 길이 경고, 한 번은 "TaskItem.tsx 파싱 에러"). **의심스러우면 새 탭을 만들어서(`tabs_create`) 재확인할 것** — 새 탭은 콘솔이 깨끗하다.
- 같은 맥락에서, dev 서버를 재시작해도(`preview_stop`→`preview_start`) 안 고쳐지는 것처럼 보이는 에러가 있었는데, `.next` 캐시를 지워도(`rm -rf .next`) 똑같았다 — 결국 진짜 원인은 코드가 아니라 위의 "오래된 탭 콘솔 로그"였다. **즉, "서버를 껐다 켜도 똑같은 에러"라고 해서 반드시 코드 문제인 건 아니다.** 새 탭부터 의심할 것
- 이 과정에서 **`taskkill /F /IM node.exe`로 시스템의 모든 node 프로세스를 껐다** (프로덕션 빌드 테스트용으로 띄운 `npm start`를 정리하려던 것). 이 PC에 이 프로젝트와 무관한 다른 Node 앱이 떠 있었다면 같이 꺼졌을 수 있다 — 사용자에게 이미 알렸음. **다음에는 특정 PID만 지정해서 끌 것**(`taskkill /F /PID <pid>`), 전체 종료 금지
- **`requestAnimationFrame`은 Browser pane이 화면에 안 보이는(hidden) 상태에서 아예 실행되지 않는다.** 사이드바 "⚙ 설정" 펼침 스크롤을 처음에 `rAF` 두 번 중첩해서 만들었다가, 테스트 중 콜백이 전혀 안 불려서 한참 헤맸다. **`offsetHeight`/`scrollHeight` 등을 읽는 순간 브라우저가 최신 레이아웃을 강제로 반영하므로(synchronous reflow), 이런 종류의 "펼친 직후 위치 계산" 로직은 rAF/setTimeout 없이 그냥 동기로 짜는 게 더 안전하다.** 실제 최종 구현(`Sidebar.tsx`, `CollapsibleSection.tsx`)도 그렇게 되어 있음
- CSS flex 레이아웃에서 `flex-1`(또는 `flex: 1`)을 쓴 자식이 있는데 그 자식에 `min-h-0`가 없으면, 형제 요소가 늘어날 때 그 자식이 우선적으로 짜부라진다(기본 `min-height: auto`가 "내용만큼은 유지"를 보장하기 때문). 이번 세션에 이걸로 두 번 당했다(body/사이드바 높이, 캘린더 그리드/패널). **"화면에 안 보이던 게 갑자기 겹쳐 보인다"는 버그 리포트를 받으면 제일 먼저 이 패턴을 의심할 것**

## 5. 작업 스타일 관련 — 이 사용자와 일할 때 지켜온 규칙

- 사용자는 **한국어**로 요구사항을 준다. 짧은 한 줄 요청이 많고, 실제로 앱을 써보면서 "이거 이상해/겹쳐" 식으로 버그를 발견하는 대로 바로바로 리포트하는 스타일이다 — **말이 짧고 화면 설명이 구체적이지 않을 때가 많으니, 브라우저로 직접 재현해서 원인을 파악한 다음 고칠 것.** 재현이 안 되면 섣불리 코드를 고치지 말고 재현 시나리오를 구체적으로 되물을 것(이번 세션에 "시작 페이지가 가끔 안 먹는다" 버그를 이렇게 풀었음 — `AskUserQuestion`으로 재현 조건을 물어서 "캘린더에 있는 상태에서 전체/오늘로 바꾸면 안 됨"이라는 정확한 시나리오를 받아냄)
- 탐색적 질문(예: "~어때?", "~필요하지 않아?")에는 추천안 + 트레이드오프를 2~3문장으로 먼저 제시하고, 확정 지시("~해줘")가 왔을 때 구현할 것
- **`requirements.md`는 항상 최신 상태로 유지**, 관련 없는 절은 임의로 안 바꿈(8절 원칙). 새 기능은 속하는 절에 자연스럽게 추가하거나 새 절 신설. 이번 세션에도 매 변경마다 꼬박꼬박 갱신했음(5.1/5.1.1/5.1.2/5.2/5.7.1/5.8절 다수 갱신, 맨 위 변경 이력 계속 추가)
- **성능 관련 작업만 `PERF.md`에 회차 추가** — 이번 세션은 UI/UX 변경이 대부분이라 `PERF.md`는 안 건드렸음
- **커밋/푸시는 명시적으로 요청받았을 때만** — 이번 세션도 요청이 없어서 전부 미커밋 상태로 남겨둠(3절 참고)
- **측정 없는 주장 금지** — 이번 세션도 DB 직접 조회(`better-sqlite3`)와 `getBoundingClientRect()`/`scrollTop` 등 실제 값 확인으로 버그를 검증했음. UI 버그는 특히 "말로만 고쳤다고 하지 말고 브라우저에서 좌표/스크롤 값을 실제로 찍어볼 것"
- 브라우저 자동화(`mcp__Claude_Browser__*`) 환경 한계 — 3회차에서 이어지는 것 + 이번에 새로 겪은 것:
  - `document.hasFocus()` 항상 `false` → blur 로직은 `dispatchEvent(new FocusEvent('focusout', ...))`로 직접 발생시켜야 함
  - Enter 네이티브 암묵 제출 잘 안 먹음 → `form.requestSubmit()` 사용
  - 물리 키 입력이 안 닿을 때 있음 → `document.dispatchEvent(new KeyboardEvent('keydown', {key:'x', bubbles:true}))`
  - `confirm()`은 `window.confirm = () => true`로 미리 덮어쓸 것
  - **토스트/타이머처럼 시간제한 있는 UI를 검증할 땐 "생성 → 액션 → 결과 확인"을 반드시 한 번의 `javascript_exec` 호출 안에서 처리할 것** — 라운드트립 지연이 타이머를 넘겨버릴 수 있음
  - **DOM 전체에 걸친 넓은 셀렉터로 정리하지 말 것** — 정리는 항상 자신이 만든 요소의 컨테이너로 스코프해서 할 것
  - **(신규) `requestAnimationFrame`은 pane이 hidden이면 실행 안 됨** — 4.3절 참고. 동기 코드로 짤 것
  - **(신규) 탭을 오래 재사용하며 여러 번 고치면 콘솔 로그가 누적된다** — 의심스러운 에러는 새 탭에서 재확인
  - **(신규) `computer` 툴의 `coordinate`는 최근 `screenshot`이 반환한 프레임 기준이다.** 뷰포트를 `resize_window`로 바꾼 직후엔 스케일이 안 맞을 수 있으니, 정밀한 클릭/호버가 필요하면 `javascript_exec`로 `getBoundingClientRect()`를 직접 읽어서 좌표를 계산하거나, 아예 DOM에 직접 이벤트를 `dispatchEvent`하는 편이 더 안정적이었다
- DB 직접 조회/수정 스크립트는 **스크래치패드 디렉토리**에만 만들 것 (커밋에 안 섞이게)
- **사용자의 실제 데이터를 함부로 지우지 않기.** 테스트는 전용 접두사(`UNDOTEST-`, `BULKUNDO_`, `ZTEST-` 등)로만 하고 끝나면 지운다. 이번 세션에 `ZTEST-` 접두사로 15일 필터 검증용 태스크를 만들었다가 확인 후 바로 지웠음 — DB에 잔여물 없음(직접 재확인함)

## 6. 다음에 할 만한 일

**이전부터 미뤄온 것 (아직 그대로)**
- **자동화 테스트가 전혀 없음**. `tsc`+`lint`+브라우저 수동 클릭으로만 검증 중. 이번 세션에 선택 로직/시작 페이지/캘린더 레이아웃처럼 회귀 위험이 큰 변경이 많아서 가치가 더 커졌음
- 프로젝트 사이드바 필터가 캘린더에 아직 연결 안 됨(캘린더에서 프로젝트 눌러도 목록 뷰로 이동)
- 캘린더 "미등록 일정" 섹션에 개수/기간 제한 없음(현재 21개, 레이아웃 겹침 버그는 고쳤지만 무제한인 건 여전함) — 완료 뷰 15일 제한과 같은 이유로 논의됐던 것

**기능 (Phase 2, `requirements.md` 6절 — 아직 아무것도 구현 안 됨)**
- 완료 통계/생산성 리포트, 자연어 날짜 파싱 확장, 다크모드 토글, 태그별 필터 UI

**이번 세션 작업 중 다시 확인해볼 만한 것 (버그는 아니지만 실측이 부족한 부분)**
- 일반/다중 선택 전환을 **목록 뷰에서만** 실측 확인함. 우선순위별/프로젝트별/태그별 칸반 레이아웃도 같은 `TaskItem`을 재사용하니 이론상 문제없지만, 실제로 칸반에서 클릭해본 적은 없음
- 시작 페이지 설정의 "즉시 이동" 수정(`router.push`)은 **dev 서버에서만** 확인함. 최초 리다이렉트 로직(쿠키→`redirect()`)은 프로덕션 빌드(`npm run build && npm start`)로도 확인했었음

**운영**
- 이번 세션 작업 전부 미커밋 — 다음 세션에서 커밋 요청이 오면 `git status`로 위 파일 목록(수정 9개, 신규 10개) 확인 후 진행할 것
