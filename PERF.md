# 성능 최적화 히스토리

> 이 문서는 **무엇을 왜 바꿨고 얼마나 빨라졌는지**를 회차별로 쌓아두는 기록이다.
> 기능 스펙의 단일 기준(SSOT)은 [`requirements.md`](requirements.md)이고, 현재 적용된 성능 설정의
> "지금 상태"는 그 문서 10절에 정리되어 있다. 이 문서는 그 10절이 **어떻게 그렇게 됐는지**의 이력이다.
>
> 다음에 최적화할 때는 맨 아래에 새 회차를 추가할 것 (기존 회차는 수정하지 않는다).

## 측정 환경

- Windows 11 / Node.js v24.19.0 / Next.js 16.3.4 (Turbopack) / Prisma 7.10.0 + better-sqlite3
- 데이터: 태스크 26건, 프로젝트 2개, 태그 1개 (실사용 DB)
- 측정 방법: 워밍업 후 15~100회 반복, 중앙값(p50)과 평균 병기

---

## 1회차 — 2026-09-08 · 반응/저장/로드 속도 전반

**요청**: "기능은 손대지 않고, 반응 속도·저장·로드 속도 최적화. 극단적으로 가볍고 빠른 앱을 원함"

**결과 요약**

| 항목 | 전 | 후 | 배수 |
|---|---|---|---|
| 페이지 서버 렌더 | 46.4ms (dev) | 14.0ms (prod) | 3.3배 |
| 단일 DB 쓰기 | 1.42ms | 0.03ms | 48배 |
| 태스크 추가 왕복 (저장→화면반영) | — | 약 10ms | — |
| 다중 선택 N개 완료/삭제 | 서버 왕복 N회 | 1회 | N배 |
| 완료/삭제 클릭 반응 | 서버 응답 후 | 즉시(낙관적) | 체감 |
| 초기 HTML | 35.4KB | 27.6KB | 22% ↓ |
| 웹폰트 요청 | Geist 2종 | 없음 | — |

기능·화면은 변경 없음. 첫 방문 전송량은 632KB / 요청 10개 (대부분 React+Next 런타임).

### 무엇을 바꿨나

#### ① 실행 방식 — 개발 서버로 앱을 쓰고 있었다 (가장 큰 원인)

`dev.cmd`(=`next dev`)로 실사용 중이었다. 개발 서버는 요청마다 라우트를 컴파일하고
최적화를 끈 React를 쓰기 때문에 같은 화면이라도 훨씬 느리다.

- 조치: [`run.cmd`](run.cmd) 추가 (`npm run build && npm start`), `package.json`에 `serve` 스크립트 추가
- 효과: **46.4ms → 14.0ms (3.3배)**, HTML 35.4KB → 27.6KB
- 주의: 코드 수정 후 자동 반영 안 됨 — 다시 실행해야 함. 개발 중에는 `dev.cmd` 그대로 사용

> 코드를 한 줄도 안 고치고 얻은 가장 큰 이득. 다음에 "느리다"는 느낌이 들면 **먼저 어떤 서버로 띄웠는지 확인할 것.**

#### ② SQLite WAL 모드 — 저장 속도

기본 저널 모드(`delete`)는 커밋마다 원본 페이지를 저널 파일에 복사 → fsync → 본 DB 수정 → fsync
→ 저널 삭제를 한다. WAL은 변경분을 `dev.db-wal` 끝에 이어붙이고 끝난다.

- 조치: [`src/lib/prisma.ts`](src/lib/prisma.ts)가 부팅 시 `PRAGMA journal_mode = WAL` 1회 설정
- 효과: **1.42ms → 0.03ms (48배)**
- 참고: `synchronous = NORMAL`도 함께 검토했으나 이 환경에서 추가 이득이 없어(0.029 → 0.028ms) **적용하지 않음**. 어댑터가 커넥션별 pragma 훅을 노출하지 않아 래퍼가 필요한데, 측정상 값어치가 없었다
- 부작용: `dev.db-wal`, `dev.db-shm` 파일 생성 (gitignore 처리). **DB 복사 시 앱을 끄거나 두 파일을 같이 옮겨야 함**

#### ③ 인덱스 5개 — 로드 속도 (지금은 체감 없음, 성장 대비)

Task/TaskTag에 인덱스가 **하나도 없어서** 모든 조회가 전체 스캔이었다.

- 마이그레이션: `20260908000049_add_indexes`
- `Task(parentId, completed, dueDate)` — 4개 뷰 전부를 하나로 커버
- `Task(parentId)` — 서브태스크 로딩
- `Task(projectId)` — 프로젝트 필터·자동정리 *(커버링)*
- `Task(completed, completedAt)` — 주간보고
- `TaskTag(tagId)` — 태그 자동정리·태그별 칸반 *(커버링)*. 복합 PK 선두가 `taskId`라 `tagId` 단독 조회는 인덱스를 못 탔음
- 효과: 5개 쿼리 전부 `SEARCH ... USING INDEX`로 전환 (이전엔 전부 `SCAN`)
- **솔직한 평가**: 태스크 26건에서는 측정 가능한 차이가 없다. 수천 건이 됐을 때를 위한 것

#### ④ 서버 왕복 줄이기

- **`revalidatePath("/")` → `refresh()`** (Next 16 `next/cache`): 전자는 이전에 방문한 다른 페이지까지 다시 불러오게 만든다. 후자는 현재 라우트만 갱신. 덤으로 주간보고 화면에서 `n` 팝업으로 태스크를 추가해도 그 화면이 바로 갱신됨 (기존엔 안 됐음)
- **일괄 액션 신설**: `completeTasks(ids)` / `deleteTasks(ids)`. `d`/`r` 단축키가 선택 개수만큼 서버 액션을 각각 호출하던 것을 **왕복 1회**로. `updateMany` 1회이므로 `completedAt`도 전부 동일 시각
- **페이지 쿼리 병렬화**: 목록/주간보고 페이지가 프로젝트·태그·우선순위색·태스크를 순차 `await` 하던 것을 `Promise.all`로
- **액션당 트랜잭션 1회**: 완료 처리 + 반복 일정 다음 회차 생성을 `$transaction`으로 묶어 커밋(fsync) 1회
- **자동정리 쿼리 축소**: `cleanupUnusedProjectAndTags`가 후보마다 `count`를 날리던 1+N 왕복을 `groupBy` 2회 + `deleteMany`로. 시그니처가 `(projectId: string|null, tagIds)` → `(projectIds: string[], tagIds: string[])`로 바뀜
- **불필요한 컬럼 조회 제거**: 액션 내부의 사전 조회에 `select`를 붙여 필요한 필드만

#### ⑤ 낙관적 UI (`useOptimistic`) — 체감 반응 속도

완료/완료취소/삭제 시 서버 응답을 기다리지 않고 행이 즉시 사라진다.

- 핵심 단순화: **완료 뷰에서는 완료 취소한 것이, 나머지 뷰에서는 완료 처리한 것이** 그 뷰의 조건을 벗어난다. 그래서 뷰를 알 필요 없이 규칙이 "해당 id 제거" 하나로 떨어짐
- 서브태스크의 완료 토글·삭제도 각 행에서 동일 처리
- 실패하면 자동으로 원래대로 복구됨 (React가 트랜잭션 종료 시 실제 서버 데이터로 대체)

#### ⑥ 클라이언트 렌더 비용

- **쓰지도 않는 웹폰트 제거**: `layout.tsx`가 Geist Sans/Mono를 불러왔지만 `globals.css`의 `body { font-family: Arial, ... }`가 덮어써서 **한 번도 렌더에 쓰이지 않았다.** `font-sans`/`font-mono` 유틸리티 클래스도 코드 전체에서 미사용 확인 후 제거 — 화면 변화 0
- **`TaskItem`을 `memo`로 감쌈**: 태스크 하나 선택 시 목록 전체가 리렌더되던 것을 달라진 행만. `TaskList`에서 `onToggleSelect`를 `useCallback`으로 안정화해 memo가 실제로 동작하게 함
- **칸반 그룹핑 1회 순회**: 컬럼마다 `tasks.filter`를 돌려 태스크×컬럼 만큼 비교하던 것을 한 번 순회하며 Map에 나눠 담는 방식으로 바꾸고 `useMemo` 적용

### 검토했으나 적용하지 않은 것

| 항목 | 이유 |
|---|---|
| `synchronous = NORMAL` | 이 환경에서 WAL 대비 추가 이득 없음 (0.029 → 0.028ms). 어댑터 래퍼가 필요한데 값어치 없음 |
| React Compiler (`reactCompiler`) | `babel-plugin-react-compiler` 의존성 추가 + 빌드 느려짐. `memo` 수동 적용으로 충분 |
| `experimental.inlineCss` | localhost에서 CSS 요청 1회 아끼는 수준. experimental 플래그를 켤 값어치가 없음 |
| `experimental.staleTimes` | 데이터가 즉시 반영돼야 하는 앱이라 클라이언트 캐시 연장은 위험 |

### 검증

- `npx tsc --noEmit` + `npm run lint` 통과
- 브라우저로 직접 클릭 확인: 태스크 생성(@/# 서버 안전망 포함) · 완료/완료취소 · **부모 삭제 시 서브태스크 cascade**(`delete` → `deleteMany` 변경으로 가장 위험했던 부분) · 프로젝트/태그 자동정리 · 반복 일정 다음 회차(1/31 → 2/7) · 일괄 `d`/`r` · 서브태스크 토글 · 칸반 3종 · 주간보고
- **데이터 무결성**: 작업 전 `dev.db`를 백업해두고, 작업 후 Task/Project/Tag/TaskTag/PriorityColor 전 테이블을 대조해 **완전 동일** 확인. 테스트는 전용 데이터(`PERFTEST-*`, `ZBULK-*`, `ZRECUR-*`, `ZLAT-*`)로만 하고 전부 삭제

### 다음에 손댈 만한 곳

지금 데이터량에서는 더 짜낼 게 거의 없다. 아래는 **규모가 커졌을 때** 의미가 생기는 것들이다.

1. **목록 페이지네이션 / 가상 스크롤** — 태스크가 수백 건 넘어가면 "전체" 뷰가 전부를 렌더한다. 지금 구조상 가장 먼저 무너질 지점
2. **태그별 칸반의 중복 렌더** — 태그가 여러 개인 태스크는 컬럼마다 `TaskItem`이 따로 생긴다. 태그 수 × 태스크 수만큼 늘어남
3. **첫 방문 632KB** — React+Next 런타임이 대부분이라 프레임워크를 안 바꾸면 크게 못 줄인다. 정말 극단적으로 가볍게 가려면 그때는 프레임워크 선택 자체를 재검토하는 이야기가 됨
4. **주간보고 페이지의 `OR` 3중 조건** — 서브태스크 존재 여부 서브쿼리가 섞여 있어 데이터가 늘면 인덱스로 커버가 안 될 수 있음

---

## 2회차 — 2026-09-08 · 데이터가 늘어도 안 무거워지게 (전반 재점검)

**요청**: "신규 기능 포함해서 전반적으로 재점검. 가볍고 라이트한 동작을 추구하는데 오래 걸리는 동작이 있으면 어떻게 바꿀지 고민하고, 고치기 전에 리스트업."

### 측정 환경 (1회차와 달라진 부분)

- Windows 11 / Node.js v24.19.0 / Next.js 16.3.4 (Turbopack) / React 19.2.8 / Prisma 7.10.0 + better-sqlite3 12.11.1
- 실사용 데이터: **태스크 35건**(최상위 32) · 프로젝트 5 · 태그 4 (1회차는 26건)
- 측정 방법: `next start`(프로덕션)로 띄우고 워밍업 8회 후 25~40회 반복, p50 + 평균 병기
- **스케일 측정은 실 DB가 아니라 스크래치패드의 합성 DB**(200/1000/3000건)로 별도 포트에서 진행. `dev.db`는 전혀 건드리지 않음

### 먼저 확인한 것: 지금 데이터량에서는 느린 곳이 없었다

35건 기준으로 서버 렌더 3.6~7.6ms, 소프트 내비 12~22ms, 태스크 선택 클릭 0.4ms(`memo` 정상 동작),
태스크 추가 왕복 17ms, 첫 방문 압축 후 약 200KB / 요청 10개. 인덱스는 8개 쿼리 전부 `SEARCH`(SCAN 0건).
**그래서 이번 회차의 주제는 "지금 느린 것"이 아니라 "늘어나면 무너지는 것"이 됐다.**

### 결과 요약

| 항목 | 전 | 후 |
|---|---|---|
| **미완료 최상위 1,000건 초과 시 목록 화면** | **500 에러** (Prisma `P2029`) | **정상** |
| 1,000건 `/?view=all` | 68.8ms · 2.07MB | **7.5ms · 160KB** |
| 1,000건 `/?view=all&layout=tag` | 156.8ms · 2.58MB | **8.4ms · 194KB** |
| 3,000건 `/?view=today` | 78ms · 2.14MB | **9.7ms · 173KB** |
| 3,000건 `/calendar` | 44.6ms · 770KB | **13.1ms · 303KB** |
| 1,000건 태스크 추가 왕복(저장→화면반영) | 약 180ms | **약 20ms** |
| 1,000건 하이드레이션 / DOM 노드 | 229ms / 17,428 | **57ms / 1,321** |
| 1,000건 사이드바 드래그 1틱 | 55.4ms (약 18fps) | **3.7ms**, 드래그 중 리액트 렌더 0회 |
| 목록 행마다 `localStorage` 읽기 | 행 수 × 2회 (234행 → 468회) | **목록당 2회** |
| 빌드 경고(프로젝트 전체 트레이싱) | 1건 | **0건** |

기능·화면은 페이지네이션 UI가 새로 생긴 것 외에 그대로다. 실사용 35건에서는 기본 페이지 크기(50) 미만이라
페이지 표시줄조차 나타나지 않고 동작이 이전과 완전히 같다.

### 무엇을 바꿨나

#### ① 목록을 DB 쿼리 단계에서 자른다 (가장 큰 변화)

조건에 맞는 태스크를 **전부** 가져와 전부 렌더하고 있었다. 화면에 보이는 개수와 무관하게
서버 렌더·직렬화·전송량·하이드레이션이 전부 데이터량에 정비례했다.

그리고 그냥 느려지는 게 아니라 **죽었다**: 관계까지 `include` 하는 조회라 Prisma가
`WHERE parentId IN (?,?,…)` 를 행 수만큼의 파라미터로 날리는데, SQLite 파라미터 한도(999)를
넘으면 `P2029`로 실패한다. 실측 임계점 — 미완료 최상위 **900건 정상 / 1,125건 500 에러**.

- 조치: `skip`/`take`로 한 페이지만 조회 (`src/app/page.tsx`), `?page=N`을 URL에 남김
- 페이지당 개수는 사이드바 "⚙ 설정"에서 직접 입력 (기본 50, 1~200). 서버가 자르므로
  `localStorage`가 아니라 **쿠키**(`mytodo_page_size`)에 저장 — 시작 페이지 설정과 같은 이유
- `count`는 목록 조회와 같은 `Promise.all`에 넣어 왕복을 늘리지 않았다. 범위를 벗어난
  `?page=`일 때만 한 번 더 조회해서 마지막 페이지로 당겨온다
- **정렬에 `id` 타이브레이커 추가**: `skip`/`take`로 자르려면 동점일 때 순서가 매번 같아야 한다.
  일괄 완료는 선택한 전부에 같은 `completedAt`을 넣고(1회차 ④), 생성일도 같은 밀리초로 겹칠 수 있다.
  동점 300건짜리 DB로 검증했을 때 이 환경에서는 타이브레이커 없이도 우연히 안정적이었지만
  (중복 0 / 누락 0), SQL이 보장하는 동작이 아니라 명시적으로 확정해 뒀다
- **페이지가 바뀌면 선택은 지금 화면에 있는 것만 남긴다**: 안 그러면 `d`/`r` 단축키가 화면에
  보이지도 않는 태스크를 일괄 처리한다. (이 회차 측정 중에 실제로 당해서 실데이터 1건을
  지웠다 — 백업으로 복원하고 전 테이블 대조로 확인. 그 사고가 이 방어를 넣은 이유다.)

#### ② 캘린더가 화면에 안 쓰는 데이터를 그만 가져오게

- **그리드**: 칸의 칩이 쓰는 건 제목·우선순위색·날짜뿐인데 프로젝트·서브태스크·태그를 통째로
  붙여 왔다. `select`로 좁힘
- **사이드 패널**: 관계가 실제로 필요한 건 "선택한 하루"뿐이라 그 하루치만 따로 조회
- **"미등록 일정"**: 화면엔 10개씩 보여주면서 서버는 전건을 조회·직렬화하고 있었다
  (`PaginatedTaskList`가 이미 받아온 배열을 클라이언트에서 자르는 방식). 목록 화면과 같은
  서버 페이지네이션으로 통일하고 그 컴포넌트는 삭제. 페이지를 넘길 때 섹션이 닫히지 않도록
  첫 페이지가 아니면 펼쳐진 채로 그린다(`CollapsibleSection`의 `defaultOpen`)
- 효과: 3,000건 `/calendar` **770KB · 44.6ms → 303KB · 13.1ms**
- **정직한 부작용**: 실사용 35건에서는 오히려 70.9KB → 85.1KB로 조금 커졌다. "미등록 일정"의
  한 페이지가 10개에서 설정값(기본 50)으로 늘어 21건이 전부 그려지기 때문. 5.0ms → 9.1ms 수준이라
  체감은 없고, 페이지당 개수를 낮추면 그대로 줄어든다

#### ③ 크기 조절 드래그에서 리액트를 뺐다

사이드바와 캘린더 분할이 `mousemove`마다 `setState`를 돌려 자기 자신과 하단 단축키 바를
통째로 다시 그렸다. 게다가 매 틱 `getBoundingClientRect()`를 불러 강제 레이아웃까지 유발했다.

- 조치: `:root`의 CSS 변수 하나만 갱신하고 관련 요소가 그 변수를 참조 (`--mytodo-sidebar-width`,
  `--mytodo-calendar-grid-percent`). 드래그 시작 시 rect를 한 번만 캐시. 저장은 드래그가 끝날 때 1회
- 효과: 1,000건 화면 기준 **55.4ms → 3.7ms**, 드래그 중 리액트 렌더 **0회**.
  `ShortcutBar`는 폭을 구독할 필요가 아예 없어져 훅 하나가 사라졌다

#### ④ 목록 공통 설정을 행마다 읽던 것 정리

"다중 선택 조합키"와 "메모 기본 펼침"을 **태스크 행마다** `localStorage`에서 읽고 `window`
리스너를 걸고, 마운트 직후 `setState`를 해서 전 행을 한 번 더 렌더했다.
계측: **234행 → 읽기 정확히 468회**(행×2).

- 조치: `TaskList`가 한 번만 읽어 props로 내려줌 (`useMemoDefaultExpanded` 신설)
- `localStorage` 읽기 자체는 싸다(3,000회에 1.4ms). 실익은 리스너 수와 마운트 직후 추가 렌더 쪽
- 부수 효과(개선): "메모 표시" 설정이 이제 화면에 **바로** 반영된다. 개별로 접거나 편 태스크는
  그 선택이 이기므로 소급 변경되지 않는 규칙은 그대로

#### ⑤ 삭제 실행취소를 한 트랜잭션으로

`undoDelete`가 복원할 행마다 `await`를 걸어 하나씩 왕복했다. 일괄 삭제는 이미 왕복 1회인데
되돌리는 쪽만 개수에 비례했다.

- 조치: `$transaction` 하나로 묶음 (프로젝트/태그 upsert → 부모 태스크 → 서브태스크 순서 유지)
- 덤: 부분 복원이 남을 수 없게 됐다(전부 복원되거나 전부 그대로)
- 검증: 400건 벤치 DB에서 서브태스크·태그가 딸린 20건을 일괄 삭제 후 실행취소 →
  Task/Project/Tag/TaskTag 네 테이블 전부 삭제 직전과 정확히 일치

#### ⑥ 빌드 경고 제거

`src/lib/prisma.ts`의 백업 로직이 런타임에 정해지는 경로로 `fs`를 호출해서, Turbopack이
"프로젝트 전체를 서버 번들에 포함"한다는 경고를 냈다(Vercel 배포 시 크기·시간 문제).
읽는 대상이 소스가 아니라 실행 중인 DB 파일이라 `/*turbopackIgnore: true*/`로 트레이싱에서 제외.

### 측정해 보고 "안 고치기로" 한 것

| 항목 | 측정값 | 판단 |
|---|---|---|
| 서버 액션의 순차 왕복 (`updateTask` 4~5회, `applyDelete` 5~6회) | 체인의 모든 쿼리가 **0.002~0.006ms** (WAL). 전부 합쳐 0.02ms 수준 | 액션 하나가 15~50ms인데 그중 DB 왕복은 1~2ms 미만. 트랜잭션으로 묶는 위험 대비 실익이 없어 그대로 둠 |
| 주간보고 "진행 중" OR 3중 조건 | 3,000건에서 **2.46ms** (결과 1,683행) | 쿼리는 문제가 아니었다. `/report`가 무거운 건 후보를 전부 렌더하기 때문이고, 그 개수는 "한 주"로 자연히 묶인다. `updatedAt` 인덱스는 OR에 안 먹고 쓰기 비용만 늘어서 추가 안 함 |
| 태그별 칸반의 중복 렌더 (1회차 숙제 2번) | 페이지네이션 이후 3,000건에서 8.9ms · 200KB | 한 페이지분만 재배열하므로 중복이 생겨도 상한이 페이지 크기다. 별도 조치 불필요 |
| 첫 방문 약 200KB(압축) / 요청 10개 (1회차 숙제 3번) | 1회차와 사실상 동일 | React+Next 런타임이 대부분. 1회차 결론 그대로 |

### 검증

- `npx tsc --noEmit` + `npm run lint` 통과, 프로덕션 빌드 경고 0건
- 브라우저 직접 확인: 페이지 이동(1↔2↔마지막·범위 초과 `?page=99` 자동 보정) · 페이지당 개수
  입력 범위 보정(500→200, 0→1, 빈 값·문자→50) · 칸반에서도 페이지네이션 동작 ·
  페이지 이동 시 선택 자동 해제 · 캘린더 그리드/패널/미등록 페이지네이션(+섹션 펼침 유지) ·
  사이드바 드래그와 하단 단축키 바 동기 · 메모 접기/펼치기와 설정 즉시 반영 ·
  Ctrl+클릭 다중선택 / 일반 클릭 단일선택 / Esc 해제 · 일괄 삭제 후 실행취소
- **데이터 무결성**: 작업 전 `dev.db`를 스크래치패드에 백업해두고, 작업 후 Task/Project/Tag/
  TaskTag/PriorityColor 전 테이블을 대조해 **완전 동일**(35/5/4/5/4) 확인.
  측정 중 실수로 지운 태스크 1건(+연쇄 삭제된 프로젝트·태그)은 원래 id 그대로 복원함
- 측정용 스크립트와 합성 DB는 전부 스크래치패드에만 두고 커밋하지 않음

### 다음에 손댈 만한 곳

1. **`/report`** — 이제 유일하게 데이터량을 그대로 받는 화면이다(3,000건에서 41.5ms · 1.35MB).
   쿼리가 아니라 "한 주의 후보를 전부 렌더"하는 구조 때문이고, 실사용에서 한 주 후보는 수십 건
   수준이라 당장은 문제가 아니다. 필요해지면 여기에도 같은 페이지네이션을 붙이면 된다
2. **캘린더 그리드** — 한 달에 걸린 태스크는 전부 조회해야 칩을 그릴 수 있다(칸당 3개만 보여주더라도).
   컬럼은 이미 최소로 좁혔고, 더 줄이려면 "칸당 상위 3건 + 개수"를 SQL에서 뽑는 방식이 필요하다
3. **가상 스크롤** — 페이지당 개수를 상한(200)으로 쓰는 사용자가 생기면 그때 의미가 생긴다.
   기본 50에서는 필요 없다
